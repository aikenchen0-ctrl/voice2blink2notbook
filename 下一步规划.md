# 下一步规划

## 总原则

不要让 FMCW 直接替代旧版 `wave/blink`。

当前更合理的架构是：

```text
旧版短窗口 score/MAD 检测
-> 触发疑似眨眼候选
-> 截取候选附近 FMCW 多轨窗口
-> FMCW 二次确认是否是 blink
-> 输出最终事件
```

也就是说：

- 旧版负责灵敏触发；
- FMCW 负责区分眨眼和干扰动作；
- 不再让 FMCW 每一帧都尝试解码完整 pattern；
- 不把 `dRMS > 固定阈值` 当成 blink 判定。

## 采集协作规则

凡是需要人工标注的新 session，流程固定为：

```text
启动实时页面
-> 人类按 b 标眨眼、按 w 标干扰/大动作
-> 等人类明确说“好了”
-> 再停止采集、读取 session、跑评估和改参数
```

没有“好了”之前，只允许检查进程和标注数量，不提前分析这轮数据。

## 当前曲线含义

页面上的几条线分工不同：

```text
绿色 blink_score：18.5k 单频旧版候选分数，是当前主触发线
绿色阈值：旧版 rolling median/MAD 自适应阈值
白色 FMCW candidate：FMCW 运动候选分数，更像“有运动了吗”
橙色阈值：白色候选分数的自适应阈值
青色 vote evidence：45 条 FMCW 轨迹投票后的置信分数
洋红 fixed trajectory：固定 phase-pair 的等效距离轨迹，单位 mm
蓝色 vote trajectory：投票代表轨迹，仅作辅助观察
紫色 dRMS：多轨整体运动强度
```

需要特别区分：

```text
FMCW 投票输入 = 多条 phase-difference trajectory
FMCW 投票输出 = pattern / confidence / score
```

所以“投票投出的那条线”应该拆成两个显示：

- `fmcw_blink_vote_evidence`：投票后的分数，不是原始数据；
- `fmcw_blink_trajectory_value`：被投票证据选中的代表轨迹末端值，只作辅助，不再当主物理轨迹。
- `fmcw_fixed_trajectory_distance_mm`：固定 pair 的连续 phase-difference trajectory 按论文 Eq. 8 换算的相对等效距离，用来检查 FMCW 原始轨迹本身是否有眨眼峰。

排查顺序：

```text
先看洋红固定 mm 轨迹是否随 b 标注明显起伏；
再看蓝色投票代表轨迹是否与洋红一致；
最后看青色 vote evidence 是否在眨眼附近升高。
```

判断：

```text
洋红有峰、蓝色/青色没有：
  优先修 candidate interval selection、segmentation、vote、representative selection。

洋红也没峰：
  优先修 FMCW 同步、phase extraction、频段、几何位置、音量/滤波。
```

## 当前默认候选参数

旧版单频 blink_score 仍是主候选触发线。当前默认值来自 `hp_fmcw_20260615_171453 + hp_fmcw_20260615_180004` 的聚合 sweep：

```text
primary_blink_peak_min_score = 0.04
primary_blink_peak_min_ratio = 0.80
primary_blink_peak_refractory_s = 1.20
primary_blink_peak_max_score = 0.25
```

历史聚合基线：

```text
fused recall = 0.958
precision = 0.442
FP = 116
negative_conflicts = 0 / 4
```

这只是当前默认基线，不是最终达标证明。原因：

```text
FP 仍然多；
w/large_motion 标注太少；
较早 session 缺少 fmcw_blink_trajectory_value，不能验证代表相位差轨迹的抑制作用。
```

下一轮采集后优先运行：

```bash
.venv/bin/python scripts/evaluate_candidate_fusion_many.py \
  sessions/hp_fmcw_20260615_171453 \
  sessions/hp_fmcw_20260615_180004 \
  sessions/<new_b_w_session> \
  --output-dir docs/experiments/candidate_fusion_aggregate_with_<new_b_w_session>
```

## 第一阶段：真实 session 回放评估

目标：把 `sessions/hp_fmcw_20260613_170254` 变成第一条真实回归基准。

### 任务

1. 新增离线分析脚本：

```text
scripts/analyze_fmcw_session.py
```

输入：

```text
features.csv
manual_markers.csv
metadata.json
```

输出：

```text
每个 marker 附近 +-0.8s 的统计
dRMS 峰值
pattern 分布
confidence 分布
轨迹峰宽
多轨一致性
是否被当前门控触发
```

2. 生成报告目录：

```text
docs/experiments/fmcw_20260613_170254/
```

建议输出：

```text
marker_windows.csv
summary.json
per_marker_tracks/*.png
drms_threshold_sweep.csv
```

3. 增加回归测试。

测试目标不是“准确率已经达标”，而是固定当前数据事实：

```text
13 个 blink marker
6 个 large_motion marker
blink 的 dRMS 多数集中在 0.08~0.12
large_motion 有低幅动作，也有 2.0+ 高幅动作
当前固定 0.10 门槛不足以区分
```

### 验收标准

- 能一键分析 session；
- 能输出 blink 和 large_motion 的对比表；
- 能说明当前误判/漏判来自哪个环节；
- 测试可重复运行。

## 第二阶段：候选触发器

目标：恢复旧版“可视角度更准”的优势。

### 方案

候选触发先不使用完整 FMCW pattern，而使用短窗口信号：

```text
old blink_score
fmcw_track_delta_rms
fmcw 多轨短时峰值
```

引入 rolling median/MAD：

```text
candidate_score -> rolling median/MAD -> candidate_threshold
```

候选触发只回答：

```text
这里是否值得让 FMCW 做二次确认？
```

不直接回答：

```text
这是否一定是眨眼？
```

### 验收标准

- blink marker 附近候选召回率高；
- large_motion 可以先允许进入候选，但必须带上特征供二次确认；
- UI 恢复一条可解释 threshold 线；
- CSV 记录 candidate score、threshold、baseline、MAD。

## 第三阶段：FMCW 二次确认

目标：用 FMCW 多轨减少干扰动作误判。

### 方案

候选触发后，截取候选附近固定窗口：

```text
t - 0.6s 到 t + 0.6s
```

或：

```text
t - 0.8s 到 t + 0.8s
```

在该窗口内运行：

```text
45 条 candidate trajectories
segmentation
voting
形态特征
```

增加区分特征：

```text
峰宽
峰持续时间
多轨同步程度
多轨方向一致性
pattern 稳定性
confidence 峰值
大动作 dRMS 上限
```

### 验收标准

- blink marker 附近能稳定出现候选；
- large_motion 的高幅动作能被抑制；
- 低幅 large_motion 和 blink 的差异能被量化；
- 输出 `fmcw_confirmed`，但先不强行作为最终事件。

## 第四阶段：实时 UI 改造

目标：页面同时保留旧版可解释性和 FMCW 多轨信息。

### 页面显示

建议显示：

```text
candidate_score
candidate_threshold
fmcw_confirm_score
fmcw_pattern
fmcw_confidence
suppressed_large_motion
```

底部仍显示 5 条轨迹，但上方恢复阈值线概念。

### 验收标准

- 静止时不乱跳 pattern；
- blink 时 candidate score 有峰；
- large_motion 时能看到 suppression 状态；
- 用户能从页面上理解为什么触发或不触发。

## 第五阶段：真实数据迭代

每一轮采集建议固定流程：

```text
静止 5s
自然眨眼 10 次，每次按 b
连续眨眼 5 组，每组按 b
转头 3 次，每次按 w
张嘴 3 次，每次按 w
挑眉 3 次，每次按 w
手靠近 3 次，每次按 w
```

每轮都生成：

```text
session summary
marker 对齐报告
候选召回率
干扰误触发率
典型失败样例
```

## 最近一个具体任务

下一步最应该先做：

```text
scripts/analyze_fmcw_session.py
```

优先级原因：

1. 已经有一条带标注 session；
2. 当前肉眼判断已经发现固定门槛不足；
3. 继续调实时参数前，必须先把 blink/large_motion 的差异量化；
4. 有了回放脚本，后面每次改算法都能马上比较。

## 第一条真实回放结果

已完成：

```text
scripts/analyze_fmcw_session.py sessions/hp_fmcw_20260613_170254 --window 0.8
```

输出目录：

```text
docs/experiments/hp_fmcw_20260613_170254/
```

结果摘要：

```text
blink marker: 13
large_motion marker: 6
features rows: 13518
```

`dRMS` 阈值 sweep：

| threshold | blink recall | large motion trigger rate |
| ---: | ---: | ---: |
| 0.07 | 1.00 | 1.00 |
| 0.09 | 0.69 | 1.00 |
| 0.10 | 0.54 | 1.00 |
| 0.12 | 0.23 | 0.83 |
| 0.15 | 0.00 | 0.83 |
| 0.20 | 0.00 | 0.33 |

结论：

```text
单独 fmcw_track_delta_rms 不能作为 blink 判定阈值。
```

低阈值能召回 blink，但 large_motion 也全部触发；高阈值能压掉部分大动作，但 blink 也被压掉。因此下一步候选触发器应该召回优先，二次确认再负责排除干扰。

更新后的下一步：

```text
实现 candidate_score 的 rolling median/MAD
但它只产生 candidate，不直接产生 blink event
```

候选触发后，再对 `+-0.8s` FMCW 窗口计算：

```text
pattern 稳定性
confidence 峰值
高 dRMS 大动作抑制
多轨峰宽
多轨同步程度
```

这些特征组合后，才进入 `fmcw_confirmed`。

## 第二条真实回放结果

已完成：

```text
scripts/analyze_fmcw_session.py sessions/hp_fmcw_20260613_172442 --window 0.8
```

输出目录：

```text
docs/experiments/hp_fmcw_20260613_172442/
```

结果摘要：

```text
blink marker: 9
large_motion marker: 5
manual marker: 1
features rows: 5570
```

`dRMS` 阈值 sweep：

| threshold | blink recall | large motion trigger rate |
| ---: | ---: | ---: |
| 0.07 | 1.00 | 1.00 |
| 0.10 | 1.00 | 1.00 |
| 0.12 | 0.67 | 1.00 |
| 0.15 | 0.33 | 1.00 |
| 0.20 | 0.00 | 1.00 |
| 0.30 | 0.00 | 0.40 |

两轮合并后的设计约束：

```text
候选触发阈值必须偏低，否则会漏 blink。
候选触发阈值偏低必然会触发 large_motion。
因此 candidate_score 只负责召回，不负责最终判断。
```

已实现：

```text
FMCWConfirmFeature
```

已计算这些字段：

```text
max_delta_rms
median_delta_rms
high_delta_duration_s
max_confidence
candidate_pattern_rows
candidate_dominant_pattern
candidate_pattern_stability
track_peak_width_s
track_sync_score
large_motion_suppressed
fmcw_confirmed
```

## 当前最新任务

```text
调校 FMCWConfirmFeature，而不是继续调 candidate detector
```

原因：

```text
candidate_threshold_k = 3.0
candidate_min_score = 0.05
```

已经让两轮真实 session 的 blink 全部进入 candidate：

| session | blink candidate hit | large motion candidate hit |
| --- | ---: | ---: |
| hp_fmcw_20260613_170254 | 13/13 | 6/6 |
| hp_fmcw_20260613_172442 | 9/9 | 5/5 |

所以 candidate 层已经完成“召回优先”的目标。继续降低或升高 candidate 阈值都不会解决干扰动作；真正问题在 confirm 层。

下一步应做：

1. 对 `confirm_features.csv` 做参数 sweep。
2. 重点分析第一轮 4 个低幅 large_motion 为什么像 blink。
3. 增加 confirm 阈值报告：

```text
max_delta_rms 上限
high_delta_duration_s 上限
track_peak_width_s 范围
track_sync_score 范围
pattern_rows / confidence 是否有帮助
```

4. 只把 confirm 结果接入实时 UI，不立即作为最终 blink 事件。

## 暂时不做

这些先不要做：

- 不直接把 `fmcw_pattern` 当最终事件；
- 不继续凭感觉调 `vote_min_delta_rms`；
- 不先加复杂机器学习模型；
- 不追求完全复现论文离线流程；
- 不在没有回放评估的情况下继续堆 UI。
## 95% 眨眼检测目标推进

目标不是让实时页面“看起来更准”，而是在带人工标记的独立 session 上达到可复现的指标：

```text
blink recall >= 95%
false positive rate 低到可交互使用
```

当前基线结论：

- 单用 FMCW 论文式 45 轨迹投票，`pattern=1` 在 blink 和 background 窗口里都大量出现，不能直接当最终检测器。
- 最近三组 FMCW 标注 session 合并后，窗口规则搜索即使达到约 95% recall，也会带来约 80% false positive rate。
- 旧版单频 detector 在纯 blink 模式更准；但在当前 FMCW 混合模式里，18.5 kHz 单频落在 18-22 kHz chirp 内，`blink_score` 明显被污染。

下一步优先级：

1. 采一组 `hybrid-split` 数据，先验证旧版候选层是否恢复。

```bash
.venv/bin/python scripts/run_hp_wave_detector.py \
  --mode fmcw \
  --fmcw-band-preset hybrid-split \
  --session-root sessions \
  --input-device 0 \
  --output-device 1 \
  --amplitude 0.02 \
  --camera-width 320 \
  --camera-height 240 \
  --camera-fps 10 \
  --no-video-record \
  --fmcw-no-phase-point-log \
  --profile
```

2. 用窗口评估脚本复盘，不依赖实时 CSV 里是否保存 phase points：

```bash
.venv/bin/python scripts/evaluate_fmcw_windows.py <session_dir> \
  --output-dir docs/experiments/<session_name>_window_eval \
  --confidence-thresholds 0.4,0.5,0.6,0.7,0.8 \
  --ignore-startup 2.0
```

3. 若 `hybrid-split` 后旧版 `blink_score_ratio` 对 blink/background 明显拉开，再把最终实时链路改成：

```text
旧版单频 rolling median/MAD 触发候选
-> 截取前后 FMCW 窗口
-> FMCW 只做大动作抑制和形状确认
-> 输出 confirmed blink
```

已完成的实时接入修正：

- `primary_blink` 来源的候选现在走“旧版为主、FMCW 负责压制”的确认路径。
- 如果 FMCW 窗口没有大动作，即使 `fmcw_track_delta_rms` 很低，也允许确认眨眼。
- 如果同一窗口出现大动作，仍输出 `fmcw_suppressed_motion`，不会盲目确认。

4. 若 `hybrid-split` 仍无法恢复旧版候选层，优先回到纯 `--mode blink` 追 95%，FMCW 暂时只保留为可视化/干扰诊断，而不是主检测链路。

## 2026-06-15 事件级评估闭环

已新增事件级评估脚本：

```bash
.venv/bin/python scripts/evaluate_events.py <session_dir> \
  --output-dir docs/experiments/<name>_event_eval \
  --tolerance 0.8 \
  --ignore-startup 2.0
```

它评估的是最终事件，而不是候选事件：

```text
FMCW: fmcw_confirmed_blink
旧版 blink: blink_candidate
```

输出：

```text
marker_matches.csv
false_positives.csv
metrics.csv
summary.json
aggregate_metrics.csv
session_metrics.csv
aggregate_summary.json
```

最近三组 FMCW 标注 session 的事件级基线：

| session | markers | events | TP | FN | FP | recall | precision | F1 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| hp_fmcw_20260615_122844 | 33 | 47 | 27 | 6 | 20 | 0.818 | 0.574 | 0.675 |
| hp_fmcw_20260615_121528 | 17 | 37 | 11 | 6 | 26 | 0.647 | 0.297 | 0.407 |
| hp_fmcw_20260615_120719 | 11 | 80 | 11 | 0 | 69 | 1.000 | 0.138 | 0.242 |
| 合计 | 61 | 164 | 49 | 12 | 115 | 0.803 | 0.299 | 0.436 |

结论：

```text
当前离 95% 目标还很远。
主要问题不是“没有触发”，而是 fmcw_confirmed_blink 误报太多，同时部分 session 仍漏检。
继续只调 FMCW vote 阈值不可靠，必须先确认旧版单频候选层在 hybrid-split 下是否恢复。
```

下一轮采集必须同时跑两套评估：

```bash
.venv/bin/python scripts/evaluate_events.py <new_session> \
  --output-dir docs/experiments/<session_name>_event_eval \
  --tolerance 0.8 \
  --ignore-startup 2.0

.venv/bin/python scripts/evaluate_fmcw_windows.py <new_session> \
  --output-dir docs/experiments/<session_name>_window_eval \
  --confidence-thresholds 0.4,0.5,0.6,0.7,0.8 \
  --ignore-startup 2.0
```

下一步执行顺序：

1. 用 `--fmcw-band-preset hybrid-split` 采一组新数据。
2. 标记真实 blink 用 `b`，标记大动作/干扰用 `w`。
3. 用事件级评估看最终输出是否接近 95%。
4. 用窗口评估看 FMCW 多轨是否能解释误报和漏检。
5. 如果 `blink_score` 恢复清晰峰值，就把最终实时链路收敛为：

```text
旧版单频 blink_score/MAD 候选
-> FMCW 大动作抑制
-> confirmed blink
```

## 2026-06-15 hybrid-split 实测进展

新 session：

```text
sessions/hp_fmcw_20260615_152200
```

数据：

```text
manual blink markers: 39
events: 73
blink_candidate: 15
fmcw_confirmed_blink: 31
```

直接评估实时事件：

| event label | recall | precision | F1 | 说明 |
| --- | ---: | ---: | ---: | --- |
| fmcw_confirmed_blink | 0.564 | 0.710 | 0.629 | final 层召回不足 |
| blink_candidate | 0.205 | 0.533 | 0.296 | 旧 primary detector 被 active 释放卡住 |
| primary_blink_peak | 0.949 | 0.755 | 0.841 | 新增可见波峰候选层 |

关键发现：

```text
hybrid-split 后 blink_score 已经和真实眨眼有明显相关。
39 个 blink marker 中，37 个在 +-0.8s 内有 blink_score 波峰超过阈值。
问题从“没有声学信号”变成“实时事件触发没有稳定吃到波峰”。
```

已修复：

```text
Twinkle peak gate 的 active release 从 threshold * ratio
改成 baseline + (threshold - baseline) * ratio
```

原因：

```text
hybrid-split 下静息 blink_score 常在 0.04~0.06。
旧 release 线约 0.02，分数很难回落到这么低，active 长时间不释放。
连续眨眼时后续峰值因此不再出事件。
```

已新增：

```text
primary_blink_peak.py
primary_blink_evaluation.py
scripts/evaluate_primary_blink.py
tests/test_primary_blink_evaluation.py
```

实时链路现在变成：

```text
单频 blink_score 曲线
-> primary_blink_peak 局部峰值候选
-> pending FMCW confirm window
-> fmcw_confirmed_blink / fmcw_suppressed_motion
```

当前默认 primary peak 参数：

```text
min_score = 0.06
min_ratio = 1.0
refractory_s = 1.05
```

这轮离线 primary peak 结果：

```text
markers=39
events=49
TP=37
FN=2
FP=12
recall=0.949
precision=0.755
F1=0.841
```

下一步：

1. 再采一轮同样 `hybrid-split` 数据，验证 primary peak 是否稳定接近 95%。
2. 这轮必须同时标 `b` 和 `w`，否则无法评估抗干扰。
3. 如果 primary peak 在新数据仍接近 95%，再调 FMCW confirm 只做二次压误报。
4. 不再让未调好的 FMCW vote 单独决定最终眨眼。

## 下一轮执行计划：保召回，再压误报

当前最新结论：

```text
primary_blink_peak 已经能在 blink-only session 达到 37/39 命中，recall=0.949。
FMCW max_delta<=0.06 可以在不损失这 37 个 TP 的情况下，把 FP 从 12 降到 8。
```

实时默认策略已调整为：

```text
blink_score 局部峰值
-> primary_blink candidate
-> 收集 0.8s FMCW confirm window
-> 若 max_delta_rms 明显过大，输出 fmcw_suppressed_motion
-> 否则确认 fmcw_confirmed_blink
```

下一轮采集命令：

```bash
.venv/bin/python scripts/run_hp_wave_detector.py \
  --mode fmcw \
  --fmcw-band-preset hybrid-split \
  --session-root sessions \
  --input-device 0 \
  --output-device 1 \
  --amplitude 0.02 \
  --camera-width 320 \
  --camera-height 240 \
  --camera-fps 10 \
  --no-video-record \
  --fmcw-no-phase-point-log \
  --profile
```

采集内容：

```text
静止 5s
自然眨眼 30 次，每次按 b
转头/张嘴/挑眉/手靠近/身体轻动 20 次，每次按 w
```

采完后必须跑：

```bash
.venv/bin/python scripts/evaluate_primary_blink.py <new_session> \
  --output-dir docs/experiments/<session_name>_primary_blink_eval \
  --tolerance 0.8 \
  --ignore-startup 2.0 \
  --min-score 0.06 \
  --min-ratio 1.0 \
  --refractory 1.05

.venv/bin/python scripts/evaluate_events.py <new_session> \
  --output-dir docs/experiments/<session_name>_event_eval \
  --tolerance 0.8 \
  --ignore-startup 2.0
```

判断标准：

```text
候选层 primary_blink_peak recall 必须继续接近或超过 0.95。
最终层 fmcw_confirmed_blink 必须比候选层 FP 更少。
w 标注附近应优先出现 fmcw_suppressed_motion，而不是 confirmed blink。
如果 recall 掉到 0.95 以下，先调 primary peak；如果 FP 仍高，再调 confirm_large_motion_delta_rms。
```

## 2026-06-15 b/w 同采后的修正计划

本轮 `sessions/hp_fmcw_20260615_171453` 说明：

```text
large_motion 压制有效：4/4 个 w 附近出现 suppressed，0/4 误确认为 confirmed blink。
primary_blink_peak 不稳定：recall=0.844，precision=0.404。
final confirmed blink 更低：recall=0.778，precision=0.479。
```

因此下一步不应继续优先调大动作门控，而应先修候选层和误报解释：

1. 对 7 个 primary FN marker 做窗口级复盘：

```text
21.976104
23.085438
29.549437
34.968104
41.112104
112.749437
115.672104
```

要看它们是：

```text
blink_score 没有峰
峰低于 min_score
峰低于 ratio
refractory 合并
人工按 b 时间偏离真实眨眼
```

2. 对 primary FP 做分段复盘，尤其是 51s-61s 大动作区间：

```text
w 已经被 final suppressed，但 primary 层仍有大量高分候选。
这说明 primary 候选层需要额外输出“疑似大动作候选”的解释，不应该只看 blink_score。
```

3. 下一版候选层建议增加两类离线 sweep：

```text
refractory_s sweep: 0.75 / 0.9 / 1.05 / 1.2
min_score + ratio 联合 sweep，同时统计 negative-marker conflict
```

4. 最终验收仍然是：

```text
event-level fmcw_confirmed_blink recall >= 0.95
precision 显著高于当前 0.479
w conflict_rate 接近 0
跨至少两轮 session 稳定
```

## 2026-06-15 下一步：弱峰 FP 专项

已完成：

```text
primary_blink_event_diagnostics.csv
primary_blink_peak_max_score
score_above_max 冷却 refractory
max_score 进入 sweep / CLI / 实时配置
```

当前事实：

```text
min_ratio=0.85 + max_score=0.25 可以把 b/w session 的候选 recall 拉到 0.956。
但 precision 只有 0.443；confirm sweep 后也只有 0.462。
剩余 FP 基本不是 w，而是非 w 区间的孤立弱峰。
```

下一步不应继续只调 `confirm_large_motion_delta_rms`，而应做弱峰 FP 的局部形状判别：

1. 给 `primary_blink_event_diagnostics.csv` 增加 blink_score 峰形特征：

```text
peak_prominence: 峰值减去前后局部谷值
rise_time_s / fall_time_s: 上升和下降耗时
half_width_s: 半高宽
local_baseline_slope: 候选前后基线漂移
pre_post_symmetry: 前后斜率是否像一次短眨眼
```

2. 先离线 sweep，不直接改实时判定：

```text
min_prominence
max_half_width_s
max_baseline_slope
score/max_score 与 ratio 的组合
```

3. 目标是保持：

```text
recall >= 0.95
FP 从 50+ 明显降到 20 以下
w conflict_rate 仍接近 0
```

4. 下轮采集建议用高召回实验配置：

```bash
.venv/bin/python scripts/run_hp_wave_detector.py \
  --mode fmcw \
  --fmcw-band-preset hybrid-split \
  --session-root sessions \
  --input-device 0 \
  --output-device 1 \
  --amplitude 0.02 \
  --camera-width 320 \
  --camera-height 240 \
  --camera-fps 10 \
  --no-video-record \
  --fmcw-no-phase-point-log \
  --fmcw-primary-blink-peak-min-ratio 0.85 \
  --fmcw-primary-blink-peak-max-score 0.25 \
  --fmcw-confirm-large-motion-delta-rms 0.10 \
  --profile
```

采集时：

```text
b: 真实眨眼
w: 挥手、转头、张嘴、挑眉、身体轻动等干扰
```

## 2026-06-15 修正：峰形不是主突破口，先补旧版 gate 复盘

已经验证：

```text
绿色 blink_score 局部峰形 sweep 只带来极小提升。
b/w session precision: 0.462 -> 0.467
```

这说明弱峰 FP 和 TP 的局部峰形高度相似，下一步不能只继续堆 `prominence / half_width` 规则。

新发现并已修复的关键缺口：

```text
历史 FMCW features.csv 的 twinkle_* 列基本为空。
原因是 _fmcw_feature_row 写的是 FMCW candidate detector 的 metrics，不是 primary blink detector 的 metrics。
现在 app.py 已改为写入 latest_fmcw_primary_blink_metrics。
```

下一轮采集后优先做：

1. 评估旧版 Twinkle gate 原始条件：

```text
twinkle_candidate_accepted
twinkle_candidate_local_peak
twinkle_candidate_rising_edge
twinkle_reject_active
twinkle_reject_refractory
twinkle_reject_large_motion
twinkle_peak_sign_changes
twinkle_peak_motion_energy
```

2. 对比两条候选层：

```text
外层 PrimaryBlinkPeakGate:
  blink_score -> local peak -> min/max score -> ratio -> refractory

旧版 Twinkle gate:
  blink_score -> rolling median/MAD -> local/rising candidate
  -> active/release
  -> sign-change gate
  -> motion ceiling
  -> suppress window
```

3. 如果旧版 gate 在新 FMCW session 上 precision 明显更高且 recall 不低于 0.95：

```text
把旧版 gate 作为 primary candidate source
外层 PrimaryBlinkPeakGate 只保留可视化/诊断，或作为高召回 fallback
FMCW confirm 继续做 large_motion / 多轨二次确认
```

4. 如果旧版 gate recall 仍不足：

```text
保留 min_ratio=0.85 高召回外层 gate
再用旧版 gate 的 reject_active / sign-change / motion_energy 作为二级 FP 特征
```

## 2026-06-15 新增：用 FMCW 相位差轨迹线做下一轮可视化验证

下一轮采集时不再只盯白线/紫线：

```text
绿线：旧版单频 blink_score，判断主候选是否对齐真实眨眼。
青线：FMCW blink vote confidence，判断投票是否在眨眼附近升高。
洋红线：固定 phase-pair 的相对等效距离轨迹，判断 FMCW 原始物理轨迹是否真的出现眨眼相关波峰/波谷。
蓝线：投票代表轨迹末端值，只辅助判断投票选择是否稳定。
```

采集后优先评估：

```text
1. blink marker 附近洋红线是否有稳定正/负峰。
2. w marker 附近洋红线是否弱于白/紫泛运动线。
3. fmcw_fixed_trajectory_pair 对应的固定轨迹是否比投票代表轨迹更稳定。
4. fmcw_blink_trajectory_pair 是否长期集中在少数 phase pair。
5. 如果某些 pair 对 blink 更稳定，把它们固化为候选 track_pairs 或专门的 confirm 特征。
```

这个步骤的目标不是立刻替代绿线，而是回答：

```text
FMCW 里是否已经存在一条比白/紫更像眨眼的相位差轨迹。
如果存在，再把它变成二次确认特征；
如果不存在，说明当前 chirp/同步/phase pair 选择仍没对准眼部微动。
```

## 2026-06-15 新增：候选层决策更新

基于 `sessions/hp_fmcw_20260615_180004` 的最新复盘：

```text
PrimaryBlinkPeakGate:
  recall=0.922 precision=0.485

twinkle_candidate_accepted:
  recall=0.529 precision=0.370

twinkle_candidate_peak + blink_score <= 0.20:
  recall=0.980 precision=0.370
```

下一步候选层不要切到 `twinkle_candidate_accepted`，因为它漏检太多。

更合理的方向：

```text
1. 保留 PrimaryBlinkPeakGate 作为较干净候选。
2. 增加 twinkle_candidate_peak 作为高召回 fallback 候选，只用于补 Primary 的 FN。
3. fallback 候选必须经过 FMCW 二次确认，不能直接报 blink。
4. FMCW 二次确认优先看新增洋红固定 mm 轨迹，而不是白/紫泛运动强度。
```

下一轮采集需要包含：

```text
b: 正常眨眼、轻眨眼、快速连续眨眼
w: 挥手、转头、张嘴、挑眉、身体轻动
```

采集后第一优先级分析：

```text
对 twinkle_candidate_peak 的 85 个 FP 做分组：
- 洋红固定 mm 轨迹是否没有 blink 形态
- fmcw_blink_trajectory_pair 是否不稳定
- fmcw_blink_vote_evidence 是否低或不在 blink pattern
- w marker 附近是否能被大动作压制
```

## 2026-06-15 新增：候选融合结论

离线融合实验已经证明：

```text
Primary + twinkle_peak fallback 可以补召回；
但仅按时间窗口融合会把 FP 放大到不可接受。
```

当前不应做：

```text
不要把 twinkle_candidate_peak 直接并入实时 blink 事件。
不要只用 fallback_exclusion 时间窗口当二次确认。
```

当前应做：

```text
1. 实时上保留 PrimaryBlinkPeakGate 作为可见主候选。
2. twinkle_candidate_peak 作为 fallback_candidate 进入 pending，不直接报 blink。
3. pending fallback 必须等待 FMCW 证据：
   - fmcw_blink_vote_evidence 是否支持 blink pattern
   - fmcw_fixed_trajectory_distance_mm 是否有稳定物理轨迹形态
   - fmcw_blink_trajectory_value 是否和固定轨迹一致
   - fmcw_blink_trajectory_pair 是否稳定
   - w marker / large motion 是否触发压制
4. 先采集包含新版洋红固定 mm 字段的 b/w session，再离线调 fallback confirm。
```

下一步实现优先级：

```text
先扩展离线评估：
  对 fallback_added_events 输出 TP/FP 诊断表；（已完成 fused_candidate_diagnostics.csv）
  加入 fmcw_blink_trajectory_* 字段统计；（已完成）
  支持按 vote evidence / trajectory span / pair 稳定性 sweep。（已完成 candidate_fusion_fmcw_sweep.csv）

再接实时：
  只有离线 sweep 证明 recall >= 0.95 且 FP 明显下降后，
  才把 fallback pending 接进 app.py。
```

当前阻塞点不是代码工具，而是数据：

```text
现有可复盘 twinkle_* 的 session 是 hp_fmcw_20260615_180004；
但它采集于洋红固定 mm 轨迹字段实现之前，没有 fmcw_fixed_trajectory_*。
下一步必须重新采集包含 b/w 标注的新 session。
```

新 session 采完后立即运行：

```bash
.venv/bin/python scripts/evaluate_candidate_fusion.py sessions/<new_session> \
  --output-dir docs/experiments/<new_session>_candidate_fusion_eval \
  --tolerance 0.8 \
  --ignore-startup 2.0 \
  --primary-min-score 0.06 \
  --primary-min-ratio 0.85 \
  --primary-max-score 0.25 \
  --primary-refractory 1.05 \
  --fallback-event-column twinkle_candidate_peak \
  --fallback-score-column blink_score \
  --fallback-max-score 0.20 \
  --fallback-refractory 1.05 \
  --fallback-exclusion 0.8
```

## 2026-06-15 新增：FMCW 最相关物理轨迹线

最新离线 ranking 结论：

```text
docs/experiments/fmcw_pair_rank_20260615_171453_180004_185205/summary.json

best_pair = 10:27
separation = 0.035
blink_hit_rate = 0.00
recommended_pair = null
```

含义：

```text
当前还没有找到“可直接接入实时页面、与眨眼高度相关”的 FMCW phase-pair 物理轨迹线。
因此下一步不是把 10:27 写进实时默认参数，而是继续排查为什么有效相位区间里的轨迹峰值没有压过背景。
```

后续路径：

```text
1. 新采集 b/w session 时，必须保留 fmcw_phase_points 或确保 audio.wav 可重建 phase matrix。
2. 每轮采集后先跑 rank_fmcw_phase_pairs_many.py，判断是否出现 recommended_pair。
3. 只有 recommended_pair 非空时，才用：
   --fmcw-fixed-trajectory-pair <pair>
   把这条物理轨迹接到页面洋红线做实时验证。
4. 如果 recommended_pair 持续为空，优先查：
   - chirp 同步是否稳定
   - phase point 有效区间是否选错
   - blink 窗口是否需要看 trajectory span/形状，而不是相邻 chirp delta peak
   - 是否需要按人脸几何重新选择 reference/target 分组
```

下一次算法单元应优先做：

```text
把 phase-pair ranking 从“相邻 chirp delta peak”扩展到“窗口内轨迹形态特征”：
  - trajectory span
  - open/close 双边形态
  - 局部峰谷方向数
  - marker 前后相位位移是否回落

原因：
  论文识别的是一段轨迹形状和投票 rhythm；
  目前 ranking 用的是 delta peak，可能把真实眨眼的低频开闭轨迹压扁成不明显的单点峰。
```

已完成第一版 shape ranking 后的新结论：

```text
shape 指标确实比 delta_peak 更好：
  hp_fmcw_20260615_185205: separation 从 0.19 提升到 0.51

但三段聚合仍不达标：
  best_pair = 28:29
  separation = 0.102
  blink_hit_rate = 0.116
  recommended_pair = null
```

下一步不要继续扩大 UI 线条数量，而是排查“眨眼窗口对齐和形态归一化”：

```text
1. 以 b marker 为中心，分别扫描 center_offset = -0.4s..+0.4s。
2. 对每个 offset 重新计算 shape ranking，确认是不是人工按键延迟导致窗口没截中。
3. 在每个窗口内输出 best pair 的平均轨迹模板：
   blink_template
   background_template
   large_motion_template
4. 如果某个 offset 下 hit_rate 明显升高，再把该 offset 纳入离线评估和实时 pending window。
5. 如果 offset 扫描仍无有效 pair，说明问题更可能在 chirp 同步/声路几何/phase point 选择，而不是检测阈值。
```

offset sweep 已完成后的结论：

```text
best offset = +0.300s
best pair = 24:25
separation = 0.193
blink_hit_rate = 0.124
recommended_pair = null
```

因此下一步应该从“全局统一 pair”转向两个更细的问题：

```text
1. per-session 最优 pair 不一致：
   171453: 24:25 但 hit=0.00
   180004: 14:16
   185205: 16:29 且单 session separation > 1.0

   这说明固定 pair 可能不是当前声路下的正确假设。

2. 模板差异需要可视化/量化：
   已输出 fmcw_pair_templates.csv；
   下一步应比较 blink/background/negative 模板的：
     - 峰值位置
     - 回落程度
     - 正负方向是否稳定
     - session 间模板是否相互抵消
```

下一执行单元：

```text
新增模板诊断脚本：
  输入 fmcw_pair_templates.csv 或直接输入 session/pair/offset。
  输出每组模板的 summary：
    peak_relative_time
    span
    endpoint_delta
    sign
    blink_vs_background_template_distance

如果发现 blink 模板在不同 session 方向相反：
  ranking 需要做 polarity alignment，而不是直接平均。

如果发现单 session 可达 separation > 1 但聚合不行：
  下一轮采集必须固定坐姿/设备位置，并记录距离/角度，否则很难达到 95%。
```

模板诊断已完成后的结论：

```text
best pair 24:25, offset +0.3s：
  blink span = 0.0990
  background span = 0.1564
  negative span = 0.4192

blink 模板并不比背景强，negative 反而更强。
```

下一步更具体：

```text
1. 先不要把 24:25 接入实时。
2. 对每个 session 分别导出模板 summary，检查单 session 内是否存在稳定 blink 模板。
3. 如果只有 185205 这类 blink-only session 表现好，而 b/w session 表现差：
   说明还缺同一姿态下的 b/w 新数据。
4. 如果所有 session 的 blink 模板都弱：
   优先回到 FMCW 物理链路，排查发射频段、同步、phase point 区间和设备几何。
```

per-session 模板诊断后的判断：

```text
固定全局 FMCW 物理线暂时不成立。

证据：
  171453 best_pair=24:25, blink_hit=0.000, blink sign=negative
  180004 best_pair=14:16, blink_hit=0.059, blink sign=positive
  185205 best_pair=16:29, blink_hit=0.280, blink sign=negative
```

下一阶段必须调整协作方式：

```text
1. 不再用旧混杂 session 继续推全局 pair。
2. 重新采一组“姿态固定”的 b/w 数据：
   - 头部位置固定
   - 电脑/耳机/麦克风位置固定
   - 每轮同时标 b 和 w
   - 至少 40 blink + 20 w
3. 采集时保留 fmcw_phase_points，不使用 --fmcw-no-phase-point-log。
4. 采完立即跑：
   - scripts/evaluate_fixed_posture_study.py
5. 若新固定姿态数据仍没有 recommended_pair：
   FMCW 不应再作为主识别线，应退回“旧版 blink_score 主检测 + FMCW 只做大动作压制/诊断”。
```

固定姿态采集命令：

```bash
.venv/bin/python scripts/run_fixed_posture_collection.py \
  --input-device 0 \
  --output-device 1 \
  --amplitude 0.02
```

注意：

```text
这个入口默认保留 fmcw_phase_points；
不要再手动加 --fmcw-no-phase-point-log。
如需额外传底层 detector 参数，放在 -- 后面。
```

固定姿态总评估命令：

```bash
.venv/bin/python scripts/evaluate_fixed_posture_study.py \
  sessions/<fixed_posture_session_1> \
  sessions/<fixed_posture_session_2> \
  --output-dir docs/experiments/fixed_posture_study_<date>
```

验收看 `fixed_posture_study_summary.json`：

```text
marker_requirements_met = true
phase_point_logging_met = true
fmcw_physical_line_met = true 或明确降级
fusion_recall_met = true
fusion_precision_met = true
negative_conflict_met = true
```

当前旧数据基线：

```text
decision = need_more_fixed_posture_labels
fused recall = 0.942
recommended_pair = null
phase_point_log_sessions = 0/3
```

## 2026-06-15 更新：先用 physical-line gate 回答“有没有那条线”

现在评估报告里有两个不同问题：

```text
best_pair / recommended_pair:
  旧的 shape/peak 排名，回答“某 pair 的窗口幅度/形状能不能区分 blink 与背景”。

best_line_pair / recommended_line_pair:
  新的 physical-line template-correlation 排名，回答“某条固定 phase-pair 物理轨迹能不能稳定复现 blink 模板”。
```

当前旧三段数据结论：

```text
best_line_pair = 11:22
best_line_blink_hit_rate = 0.083
recommended_line_pair = null
```

因此下一步不应把 `11:22` 或 `24:25` 直接接入实时主检测。

下一轮采集后的验收顺序：

```text
1. `marker_requirements_met = true`
   至少 40 个 b，20 个 w。

2. `phase_point_logging_met = true`
   采集必须保留 fmcw_phase_points。

3. `recommended_line_pair != null`
   只有这一步成立，才说明找到一条可实时显示/接入的 FMCW 眨眼物理线。

4. `fusion_recall >= 0.95`
   旧版 blink_score 主触发 + FMCW 压制/确认达到目标召回。

5. `negative_conflict_met = true`
   w 标注附近不能大量误报眨眼。
```

如果第 3 步仍失败：

```text
保留 FMCW 作为大动作压制和诊断；
不要强行把某条 FMCW 线当 blink 主证据；
重点调旧版 blink_score 的阈值、候选峰值和 w 抑制。
```
