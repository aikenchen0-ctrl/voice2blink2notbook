# TwinkleTwinkle 论文对齐记录

日期：2026-06-15

## 当前目标

把本项目从“能实时看到一些声学眨眼信号”逐步推进到“尽量贴近 TwinkleTwinkle 论文链路，同时保留本机实时可视化和旧版单分数 detector 的优势”。

当前不再把 `fmcw_candidate` 当最终眨眼。实时链路应保持：

```text
旧版单频 blink score 负责候选触发
-> FMCW 保存完整 phase matrix
-> 候选窗口内重算 45 条 phase-pair trajectories
-> segmentation + hierarchical vote 给出确认证据
-> confirmed blink / suppressed motion
```

## 论文原始链路

TwinkleTwinkle 的核心处理链路是：

```text
18-22 kHz FMCW chirp
-> 每周期 512 samples：480 chirp + 32 guard
-> Tukey window
-> 接收信号先高通，移除 audible noise
-> 接收信号乘 cos(phi_t) 和 -sin(phi_t) 得到 I/Q baseband
-> decimation factor = 16，完整 512 点周期得到 32 个 phase points
-> target phase points 只使用 active chirp 内 index > 15 的有效区间
-> phase unwrap
-> 选择多个 phase-pair，做 phase subtraction
-> normalize + small moving average smoothing
-> 三种准则选 9 个 phase intervals
-> 每个 interval 5 条轨迹，总计 45 条 candidate trajectories
-> 每条轨迹做 extrema edge segmentation
-> adaptive constraints 过滤干扰边
-> over-pruning correction 补回连续眨眼中间弱边
-> 每条轨迹解码为 1/2/3 blink rhythm
-> 3 组各 15 条先投票，再在三组 winner 中投票
```

## 当前实现对齐表

| 论文步骤 | 当前代码 | 状态 | 备注 |
| --- | --- | --- | --- |
| 18-22 kHz FMCW | `dsp.fmcw_chirp_period` | 已修正 | 默认 18k 到 22k；发射信号按论文 §3.1 使用 `cos(phi_t)` |
| 512 周期、480 chirp、32 guard | `FmcwConfig` | 已实现 | 与论文默认一致 |
| Tukey window | `dsp.tukey_window` | 已实现 | 用于减小 chirp 跳变可闻泄漏 |
| 接收端 raw high-pass | `dsp.highpass_filter` / `FmcwStreamProcessor.extract_phase_points` | 已实现 | 论文 6.4.4 提到先高通去可闻噪声；截止频率未给出，默认 17 kHz |
| 接收端 I/Q baseband | `FmcwStreamProcessor.extract_phase_points` / `decimate_baseband_lowpass` | 已修正 | 复参考混频后做 windowed-sinc FIR 低通，再 decimation |
| 录音 chirp 周期同步 | `RealtimeHandWaveApp._estimate_fmcw_sync` / `FmcwStreamProcessor.period_start_offset` | 已实现 | macOS 实时 I/O 有输入输出延迟；先估计录音里的 chirp 起点偏移，再切帧取 phase points |
| decimation=16 | `FmcwConfig.decimation_factor` | 已修正 | 完整 512 点周期抽取 32 个 phase points；候选只用 active chirp 的有效区间 |
| 排除 index <= 15 的 target phase point | `FmcwConfig.valid_start` | 已修正 | 论文写 “indexes larger than 15”；默认 target 从 16 开始，reference 为 15 |
| 使用后半段有效相位点 | `FmcwConfig.valid_stop` / `cli.config_from_args` | 已修正 | 默认随 `chirp_samples // decimation` 自动变化，不把 guard phase points 纳入候选 |
| phase unwrap | `extract_phase_points` / `select_fmcw_candidate_bundles` / `phase_difference_trajectory` | 已修正 | 先对 chirp 内相位点 unwrap，再做 phase subtraction；得到的时间轨迹再沿时间 unwrap |
| phase-pair trajectories | `phase_difference_trajectory` / `FmcwStreamProcessor._track_value` | 已修正 | 离线候选轨迹保留 shape 归一化；实时展示轨按 phase-point gap 归一化 |
| normalize + smoothing | `normalize_trajectory` / `smooth_trajectory` | 已修正 | 默认只做论文明确的归一化和小窗口移动平均；detrend 变成显式 A/B 选项 |
| 三准则选 phase intervals | `select_fmcw_candidate_bundles` | 已修正 | similarity / stability / same-point temporal consistency；其中 stability 按论文 4.2 合并 chirp 内斜率方差和跨 chirp 同点变化 |
| 45 条 trajectories | `select_fmcw_candidate_bundles` | 已实现 | 3 准则 * 3 interval * 5 trajectories |
| coarse segmentation | `fmcw_blink.coarse_segments` / `build_edges` / `trim_edge_starts` / `segmentation_max_gap` | 已修正 | extrema 切边 + 相对阈值；3 个采样点以上的线段才作为候选 edge；起点剪到阈值跨越后的样本；edge 分组按论文 0.5s 换算 |
| fine adaptive constraints | `fine_select_segments` | 已修正 | 按论文比例筛边后重新按时间间隔分组 |
| over-pruning correction | `over_pruning_correction` | 已修正 | 只补与 refined segment 时间范围重叠的中间弱边 |
| decode 1/2/3 | `decode_segment` / `fallback_count_from_variance` | 已修正 | 只对论文明确方向序列直接解码；5 个方向等异常序列进入方差 fallback |
| hierarchical vote | `hierarchical_vote_by_groups` / `decode_candidate_bundles` | 已修正 | 按 4.2 的三个 criterion 显式分组，各组先取 winner，再选组内票数最高的 winner |
| 论文式窗口重算 vote | `fmcw_blink.decode_phase_matrix_vote` | 已实现 | 离线分析和实时 confirm 共用同一入口 |
| 实时最终检测 | `app._update_fmcw_confirm_state` | 部分实现 | 已接入候选窗口 45 轨迹 vote，但默认仍保留工程门控和旧版候选召回 |

## 本次已改

1. `valid_stop` 默认从 `25` 改为 `30`，`valid_start` 默认从 `15` 改为 `16`。

原因：论文说 512 点周期经 decimation 后每 chirp 有 32 个 phase points，并且只比较 index larger than 15 的 phase points；本项目 active chirp 是前 480 点，所以有效候选上界仍是 `480/16=30`。旧默认 target 从 15 开始并只用到 24，一方面包含了边缘点 15，另一方面丢掉了 25..29 的候选相位点。现在完整 phase matrix 保存 32 点，但候选 target 是 16..29，reference 是 target 前一个点。

2. `FmcwFeature` 新增 `phase_points`。

每个 chirp 的完整相位点现在会进入实时 feature，不再只保留 5 条展示轨迹。

3. `features.csv` 和 `manual_markers.csv` 新增 `fmcw_phase_points`。

后续真实采集可以离线复现论文链路：

```text
marker 周围窗口
-> phase matrix
-> 45 trajectories
-> segmentation
-> hierarchical vote
```

4. 离线分析新增 `paper_style_vote_from_rows`。

它只在 session 中有完整 `fmcw_phase_points` 时生效。老 session 缺少这个字段，会返回空摘要，不会假装能复现论文 vote。

5. 实时 confirm 新增候选窗口 paper-style vote。

候选窗口收满时，实时 app 会从窗口内完整 phase points 重算：

```text
fmcw_confirm_window_pattern
fmcw_confirm_window_confidence
fmcw_confirm_window_vote_score
fmcw_confirm_window_candidate_count
fmcw_confirm_window_group_winners
```

这些字段会写入 CSV/marker，并显示在 UI 状态行中的 `win=` 和 `conf=`。当前它们先作为二次确认证据；默认不强制压过旧版候选触发，避免未实测前再次造成大量误报/漏报。

6. 候选轨迹选择进一步对齐论文 4.2。

本次修正了两个细节：

```text
phase matrix
-> chirp 内 unwrap 后计算 phase-pair trajectory 和 slope stability
-> 同一 phase point 跨 chirp 再做时间方向 unwrap，用于 temporal consistency
-> 如果三种准则选到重复 interval，优先切到 t2-2 / t2-3 reference
```

这样避免把正常的 +/-pi 相位包裹误当成时间噪声，也更符合论文“同一 interval 换 reference 保留多视角”的描述。

7. 分割与过剪枝进一步对齐论文 4.3.2 和 4.3.4。

细分割得到保留边集合 `S2` 后，现在会重新按 0.5s 对应的 `max_gap` 分组。过剪枝也只会补回 refined segment 起止范围内的中间弱边，不再把窗口外的边补回来。

这一步主要针对误报：如果被删掉的干扰边把两段运动隔开，剩余边不应继续被拼成一个 blink segment。

8. 投票层进一步对齐论文第 5 节。

论文不是把 45 条轨迹混成一个总票池，也不是简单看三个 group winner 的多数派，而是：

```text
每 15 条 trajectory 先得到一个 group winner
-> 比较三个 group winner 在各自组内的出现次数
-> 取组内票数最高的 winner 作为最终 rhythm
```

因此当前 `fmcw_vote_score` / `paper_vote_score` 表示“最终 winner 在获胜组 15 条里的票数”；`confidence` 表示 `score / 15`，不是 `score / 45`。

9. CLI 的 `valid_stop` 进一步对齐不同 chirp 长度。

论文第 6.4.3 提到长距离时可把周期扩到 `1024`、chirp 扩到 `960` 来提高 SNR。现在如果命令行没有显式传 `--fmcw-valid-stop`，程序会自动使用：

```text
valid_stop = fmcw_chirp_samples // fmcw_decimation
```

所以默认 `480/16 -> 30`，长 chirp `960/16 -> 60`，不会继续只用前 30 个 active 相位点；完整 phase matrix 仍会额外保留 guard 区抽取出的点，便于调试但不参与候选 interval。

10. 接收端 baseband 降采样进一步对齐论文 3.2。

论文接收端是：

```text
received chirp
-> 乘 cos(phi_t) 和 -sin(phi_t) 得到 I/Q
-> low-pass filter
-> decimation / polyphase decimation
-> baseband phase points
```

当前实时实现改为：

```text
received chirp
-> 乘 exp(-j phi_t) 形成复 baseband
-> windowed-sinc FIR low-pass
-> 按 decimation factor 抽取 phase points
```

这比之前“每 16 点直接均值”更接近论文。它仍是实时轻量近似，不是完整 MATLAB/polyphase filter 复现。滤波强度可用 `--fmcw-decimation-filter-taps-per-phase` 调整，默认 `8`。

11. 实时多轨展示进一步对齐论文 Eq. 6-8。

论文公式里：

```text
phase difference = 2pi * tau * B * (N2 - N1) / N
```

也就是说，同样的距离变化下，相位差会随两个 phase point 的间隔 `(N2 - N1)` 变大。旧实时可视化直接画：

```text
phi[target] - phi[reference]
```

因此 `15:20` 会天然比 `15:16` 大约放大 5 倍，不利于比较多轨形态，也会影响 `mean/max_track_delta` 这类候选分数。现在实时 `track_0..track_4` 默认会除以 phase-point gap，变成更可比的距离变化代理。

这不改变 45 条候选轨迹投票的 shape 归一化逻辑。若要恢复旧可视化尺度，可启动时加：

```bash
--fmcw-no-track-gap-normalization
```

12. 混合实时模式新增单频清理。

当前默认 FMCW 模式为了保留旧版单频 blink detector 的召回，会同时发：

```text
18-22 kHz FMCW chirp + 18.5 kHz single tone
```

论文没有这条 18.5 kHz 单频。为了让旧版候选和 FMCW phase matrix 能同时工作，现在在 FMCW 解调前默认会从 active chirp 里投影移除已知单频：

```text
recorded active chirp
-> remove known 18.5 kHz tone
-> multiply exp(-j phi_t)
-> low-pass + decimation
-> phase points
```

这是工程补偿，不是论文步骤。若要做 A/B，可加：

```bash
--fmcw-no-primary-blink-tone-cleanup
```

13. 离线分析新增 45 条轨迹明细。

论文第 5 节的最终结果来自 45 条 candidate trajectories 的分组投票。现在 `scripts/analyze_fmcw_session.py` 除了 `confirm_features.csv` 的最终摘要，还会输出：

```text
marker_trajectory_details.csv
```

每行对应某个 marker 窗口内的一条候选轨迹，包含：

```text
criterion / reference_index / target_index / vote_group / pattern / trajectory_span / peak_index
```

这一步不改变实时算法，只用于定位问题：

```text
blink 没确认：看 45 条轨迹是否多数为空或投成非 blink
large_motion 误报：看是全部轨迹都像 blink，还是某个 criterion/phase-pair 带偏投票
```

14. 方向序列解码收紧到论文 4.3.3。

之前代码把 5 个压缩方向也直接按“2 次眨眼”处理，这比论文描述更宽松。现在直接解码只覆盖：

```text
1 次：1 个方向、或一次开闭形成的 2/3 个方向
2 次：4 个交替方向
3 次：6 个交替方向
```

5 个方向、7 个方向等异常形态不再按长度猜，而是进入论文提到的 window=4、step=2 方差形态 fallback。fallback 已拆成两步：

```text
segment samples
-> window=4 / step=2 variance profile
-> count local high-low-high variance motifs
```

论文没有给出 high/low 的数值阈值，所以代码只在当前 segment 内用相对分位数判定低谷和两侧高方差 shoulder，并要求 high 与 low 有最小跨度。这样会牺牲一点宽松召回，但能减少把异常干扰边误解成多次眨眼。

15. 第 5 节层级投票改为按 criterion 显式分组。

论文第 5 节说每个 phase interval 选择准则对应 15 条 candidate blink rhythms，因此三组来自：

```text
internal_similarity
slope_stability
same_point_temporal_consistency
```

之前 `hierarchical_vote(patterns)` 只能按传入顺序三等分，隐式依赖 `select_fmcw_candidate_bundles` 的输出顺序。现在 `decode_candidate_bundles` 会按 `bundle.criterion` 分组，即使 bundle 顺序变化，也仍按论文三准则投票。

16. 重复 interval 的替代 reference 不再回退到低通边界风险区。

论文 4.2 说 phase point index 小于 15 可能受 low-pass filter 影响，只比较 index larger than 15 的 phase points；同时重复 interval 可把 reference 从 `t2-1` 改成 `t2-2/t2-3`。当前实现保留替代 reference，但加了下界：

```text
reference_index >= valid_start - 1
```

默认 `valid_start=16` 时，reference 最早是 15；target 从 16 开始。这样不会为了消除重复 interval，把 reference 回退到 14/13 这类论文提示的低通风险点。

17. 接收端新增 raw high-pass。

论文 6.4.4 在讨论 audible noise 时说明，接收信号会先通过 high-pass filter 去掉可闻背景噪声。当前 FMCW 相位提取链路更新为：

```text
recorded active chirp
-> raw high-pass
-> remove known 18.5 kHz tone
-> multiply exp(-j phi_t)
-> low-pass + decimation
-> phase points
```

论文没有给出 high-pass 截止频率和滤波器阶数。当前默认：

```text
cutoff = 17 kHz
taps = 65
```

这属于工程近似，原因是论文 chirp 从 18 kHz 开始，17 kHz 能压掉大多数可闻声，同时给 chirp 下沿留出过渡带。可用以下参数做 A/B：

```bash
--fmcw-no-raw-highpass
--fmcw-raw-highpass-hz 17000
--fmcw-raw-highpass-taps 65
```

16. 发射 chirp 改为论文定义的 cosine phase。

论文 §3.1 写的是：

```text
S_t(t) = A cos(phi_t(t))
```

之前代码用 `sin(phi_t)`。对 phase difference 来说，这主要是常量相位偏移，很多情况下会被相减抵消；但为了减少和论文的无谓差异，现在 `fmcw_chirp_period` 已改为 `cos(phi_t)`。默认 Tukey window 会把首尾压到 0，所以不会因为 cos 在 t=0 非零而引入明显跳变。

17. coarse segmentation 的 edge trim 修正。

论文 4.3.1 说，若 edge 前端有密集点，应把 start point 修剪到：

```text
first sample whose difference from the previous point is larger than 1/4 of the largest difference on the edge
```

之前实现用的是触发差值的前一个样本，实际少剪了 1 个点。现在 `trim_edge_starts` 会把起点移到差值跨越后的样本；如果剪完边长不足，则仍保留原边，避免制造过短无效 edge。

补充：论文用词是 `larger than 1/4`，因此当前触发条件是严格大于阈值；刚好等于 `1/4 * max_diff` 的差值不会触发 trim。

18. FMCW 录音周期同步。

论文实验里会先等待约 1 秒做同步，后续处理默认知道每个 received chirp 的周期位置。本项目在 macOS 上用实时音频回调同时播放和录音，录音里的 chirp 会叠加：

```text
系统输入/输出缓冲延迟
扬声器到麦克风传播延迟
PortAudio 设备延迟
```

如果直接把录音块的第 0 个样本当作 chirp 起点，`recorded active chirp -> phase points` 可能从错误位置切帧，导致整条 FMCW phase matrix 不稳定。现在 FMCW 模式启动后会先用前几块录音和本地 chirp 周期做相关，估计录音中的周期偏移：

```text
recorded warmup samples
-> average by candidate lag modulo 512
-> correlate with emitted chirp period
-> lock fmcw_sync_lag_samples when confidence >= threshold
-> FmcwStreamProcessor 按 period_start_offset 切 chirp 帧
```

这一步是论文同步假设在本机实时系统里的工程补齐，不改变旧版 `wave` / `blink`。新 session 会写出：

```text
fmcw_sync_lag_samples
fmcw_sync_confidence
```

若要做 A/B，可用：

```bash
--fmcw-no-sync
--fmcw-sync-warmup-blocks 8
--fmcw-sync-min-confidence 0.12
```

19. 论文 4.2 候选相位区间三组评分拆分。

论文 4.2 选择 45 条候选轨迹时，文字描述可拆成三组视角：

```text
轨迹之间更相似
chirp 内相邻 phase points 的 slopes 更稳定
同一 phase index 在不同 chirp 上随时间变化更均匀
```

之前 `slope_stability` 曾把后两项合并成：

```text
sum(var(adjacent slopes across chirps))
+ sum(var(same-index temporal steps across chirps))
```

这样会让第二组和第三组都偏向时间稳定，削弱 45 条轨迹的多视角作用。现在改为：

```text
internal_similarity: 5 条轨迹两两距离和
slope_stability: sum(var(adjacent slopes across chirps))
same_point_temporal_consistency: sum(var(same-index temporal steps across chirps))
```

这样更贴近论文“每个 criterion 取 3 个 interval，每个 interval 5 条轨迹”的分组投票结构，也方便从 `marker_trajectory_details.csv` 定位是哪一组带来误报。

20. 论文 4.2 同一 phase index 跨 chirp 差值打分修正。

论文原文说稳定候选区间要看：

```text
the sum of differences between the same index phase points on different chirps
```

之前 `same_point_temporal_consistency` 用的是相邻 chirp step 的方差；这会把大幅但线性的漂移误判为“稳定”，因为步长方差接近 0。现在改为：

```text
sum(abs(diff(phase[:, target_indices], axis=0)))
```

也就是同一 phase index 在相邻 chirp 间变化越小，候选分数越低；跨 `±pi` 的相位仍先 unwrap，避免把正常连续变化误算成大跳变。

21. 重复 interval 的工程退路已显式记录。

论文 4.2 只说重复 interval 时把 reference 从 `t2-1` 换成 `t2-2/t2-3`，没有说明这些替代 reference 也不可用时怎么办。当前实现的工程退路是：

```text
如果同一个 target interval 的替代 reference 越界或已被使用
-> 继续找该 criterion 排名中的下一个 unused interval
-> 尽量保留每个 criterion 3 个 interval / 15 条 trajectory
```

这不是论文新增规则，而是为了连续实时系统在边界配置下仍尽量保持第 5 节投票输入完整。

22. 论文 4.3.1 flat extrema 切边修正。

论文说 coarse segmentation 先按 alternating local minima and maxima 把 trajectory 切成 line segments。真实轨迹可能出现平顶/平底 tip；如果直接用相邻差分符号变化，极值会落在平台左端，导致 edge length 和 representative diff 偏移。现在 `detect_extrema` 会把连续同值段压缩成 run，并在 run 前后趋势反转时取平台中点作为 local max/min。

这不改变论文阈值，只是把“局部极值”在离散平台上的位置定义清楚。

23. 论文 4.3.1 coarse edge 长度阈值按方向独立。

论文说 edge length 用“each direction of each trial”的 longest edge 作为参考，而 representative difference 用全 trial 的 max difference 作为参考。当前 `filter_edges` 保持这个语义：

```text
length threshold: edge.length >= 20% * longest_by_direction[edge.direction]
diff threshold: edge.representative_diff >= 20% * max_representative_diff_in_trial
```

已补测试防止把 length 阈值误改成全局 longest edge。这样短方向里的最长边不会因为反方向存在更长边而被错误删掉。

24. 论文 4.3.1 representative difference 使用 top-3 平均值。

论文说一条 edge 上至少 3 对采样点会快速变化，因此 representative difference 是相邻采样差值里 top 3 large difference values 的平均值。当前实现为：

```text
sort(abs(diff(edge_samples)), descending)
-> mean(top 3)
```

已补测试防止误改成单个最大值或全部差值平均值。

25. 论文 4.3.2 two-edge segment 保留规则。

论文细分割中，当一个 segment 正好有两条 edge 时，如果两条边长度都超过全局最长边的一半，或者任一 edge 的 representative difference 超过全局最大 representative difference 的 40%，就保留这两条边。当前实现对应：

```text
(first.length >= max_length / 2 and second.length >= max_length / 2)
or first.representative_diff >= max_diff * 0.40
or second.representative_diff >= max_diff * 0.40
```

已补测试覆盖“长度不够，但任一 edge diff 达到 0.4 * global max_diff 时保留两条边”的情况。

26. 论文 4.3.4 over-pruning correction 的候选边范围修正。

论文说补边范围是：

```text
within the start and end points delineated by the fine segmentation
```

之前实现要求 coarse edge 完全落在 refined segment 内：

```text
edge.start >= refined.start and edge.end <= refined.end
```

这会漏掉一种情况：fine segmentation 的边界因为 edge trim 已经移到原边中部，某个被剪掉的中间边与 refined 时间范围重叠，但不是完全包含。现在改为只要时间范围有重叠就纳入候选：

```text
edge.end >= refined.start and edge.start <= refined.end
```

随后仍按论文的 40% second-longest / 50% second-largest 规则筛选，不会把远离 refined 范围的粗噪声边补回来。

21. 论文 3.2 trajectory 预处理顺序修正。

论文写的是：

```text
phase unwrapping on all samples
-> phase subtraction
-> normalize phase difference changes
-> small window moving average
```

之前 `phase_difference_trajectory` 在底层直接调用路径里可能先相减再 unwrap。现在改为：

```text
phase matrix
-> chirp 内 unwrap
-> target - reference
-> 沿时间方向 unwrap trajectory
-> normalize
-> edge-padded moving average
```

这样避免 fast-time 相位点刚好跨过 `+pi/-pi` 时，把很小的相位差误看成接近 `2pi` 的大跳变。

22. 默认关闭 trajectory detrend。

`trajectory_detrend_window` 默认从 `31` 改为 `1`。原因是论文 3.2 没有去趋势步骤；去趋势会改变 blink 开闭这种较低频的形状，尤其在连续实时窗口边界附近可能制造或压低峰。现在它保留为工程 A/B 参数：

```bash
--fmcw-trajectory-detrend-window 31
```

小窗口平滑仍默认 `3`，并改为 edge padding，避免 `np.convolve(..., mode="same")` 的零填充把轨迹两端拉低。

23. 离线分析读取 session metadata。

`scripts/analyze_fmcw_session.py` 现在会从 `metadata.json` 读取采集时的 `fmcw` 配置。这样历史 session 仍按当时的 `trajectory_detrend_window=31` 等参数复盘，新 session 才按当前论文式默认 `1` 复盘，避免把旧数据按新默认重解释。

24. 实时 confirm 决策顺序修正。

论文第 5 节最终依赖 45 条候选轨迹的分组投票；本项目是连续实时检测，还额外加了大动作门控。之前实时 confirm 先看 `max_delta_rms >= confirm_large_motion_delta_rms`，导致真实 blink 如果 FMCW delta 偏大，即使窗口 vote 很稳定也会先被压成 `suppressed_motion`。

现在顺序改为：

```text
candidate window
-> 若 45 轨迹窗口 vote 稳定支持 blink，先 confirmed_blink
-> 否则再用 large_motion_delta / high_delta_duration 做 suppressed_motion
-> 最后按旧版候选召回策略决定是否宽松确认
```

这仍是连续实时工程层，不是论文原文步骤；但它更符合论文“最终看多轨投票”的优先级。

离线 `confirm_features.csv` 也已同步这个顺序：如果 marker 窗口内的 paper-style vote 支持 blink，就不会因为 `max_delta_rms` 偏大而先标为 `large_motion_suppressed`。这样页面行为和离线分析结论保持一致。

25. 论文 4.3.1 edge 分组间隔改为时间语义。

论文写的是：

```text
If the time interval between the start point of two edges is less than 0.5 s,
they are classified in the same segment.
```

之前实现默认 `max_gap=47`，在 48 kHz、512 samples period 下近似等于 `0.5s / 10.67ms = 46.875`，但如果后续改成论文 6.4.3 提到的 `1024` 周期，固定 `47` 就会变成约 1 秒。现在新增：

```text
segmentation_group_gap_s = 0.5
segmentation_max_gap(config) = ceil(0.5 * sample_rate / period_samples) - 1
```

因为论文写的是 “less than 0.5s”，离散换算使用严格小于阈值的最大行数：

```text
max_gap = ceil(0.5 * sample_rate / period_samples) - 1
```

默认 `512/48k` 是 `46` 行，约 `0.491s`；`period_samples=1024` 时是 `23` 行。可用下面参数做 A/B：

```bash
--fmcw-segmentation-group-gap 0.5
```

26. 论文 4.3.1 candidate edge 最短长度修正。

论文说：

```text
keeping lines containing more than two sampling points as candidate edges
```

这里的条件是“采样点数 > 2”。之前实现用 `end - start <= 2` 过滤，实际把恰好 3 个采样点的线段也丢掉了。现在改为只丢掉 1 或 2 个采样点的线段：

```text
sample_count = end - start + 1
keep if sample_count > 2
```

后续 20% 相对长度、20% representative difference、fine selection 仍会继续过滤弱边；这个修正只是避免在 coarse segmentation 的第一步过早删除论文允许的候选边。

## 为什么这一步优先

之前多轨 FMCW 准确率不如旧版单轨道，根本问题之一是我们没有把论文需要的完整相位矩阵留住。只看 5 条实时展示轨迹，无法回答：

```text
这个 blink 窗口里的 45 条轨迹投票结果是什么？
这个 large_motion 为什么也投成 blink？
哪些 phase interval 更干净？
valid_start/valid_stop 改动是否改善了真实数据？
```

所以先补相位点日志和离线 paper-style vote，是继续调算法前的必要基础。

## 当前仍未完全对齐的地方

1. 论文是 trial/window-based pattern recognition，本项目是 continuous realtime detection。

论文中用户按节奏输入 1/2/3 组合，系统处理一段试验窗口；当前应用要连续实时检测单次 blink，因此必须有候选触发、confirm state machine 和大动作门控。这是工程场景差异，不可能完全照搬。

2. `decode_segment` 的异常形态 fallback 已按论文结构补齐，但阈值仍是工程选择。

论文提到异常方向序列时，用窗口大小 4、步长 2 的方差形态来估计 blink 数。当前实现已经显式生成 `variance_profile` 并只计数局部 high-low-high motif；但论文没有给出数值阈值，所以仍使用 segment 内相对分位数和最小跨度约束。该 fallback 只在明确方向序列无法解码时启用，避免覆盖原有 1/2/3 方向规则；其中 5 个方向等缺边形态不会再直接按长度猜。

3. 实时 confirm 已接入候选窗口 45 轨迹 vote，但还没有完成参数校准。

现在还需要用真实标注数据检查：

```text
fmcw_sync_confidence 是否足够高，lag 是否稳定
blink 是否经常得到 45 条候选轨迹和稳定 win pattern
large_motion 是否也被稳定误投成 blink
window vote confidence / score 是否能作为硬条件
segmentation_group_gap_s 是否需要围绕 0.5s 做小范围 A/B
```

4. 大动作抑制仍偏粗。

论文第 7.3.2 明确承认大幅头动/旋转会淹没眨眼信号。本项目不应承诺完全抗大动作，而应把目标设为：

```text
静止或准静止下高准确
中高幅干扰动作尽量 suppressed
低幅表情/头动通过数据继续区分
```

5. 当前默认 FMCW 模式仍是工程混合发射，不是纯论文发射。

为了保留旧版单频 blink 的高召回，`--mode fmcw` 默认同时发：

```text
18-22 kHz FMCW chirp + 18.5 kHz single tone
```

论文链路本身是纯 FMCW chirp。若要做严格论文对齐实验，应使用：

```bash
--fmcw-no-primary-blink
```

或者把 `--fmcw-primary-blink-tone-ratio 0`。混合模式适合当前实时应用探索；虽然现在默认会清理已知单频，但它仍不是论文中的纯 FMCW 采集。

## 下一步执行单元

1. 启动页面采一轮新数据。

必须用新版本，因为只有新 session 会保存 `fmcw_phase_points`。

2. 对新 session 运行：

```bash
.venv/bin/python scripts/analyze_fmcw_session.py sessions/<new_session> --window 0.8
```

3. 查看 `confirm_features.csv` 中这些字段：

```text
paper_vote_pattern
paper_vote_confidence
paper_vote_score
paper_vote_candidate_count
paper_vote_group_winners
```

4. 查看 `marker_trajectory_details.csv` 中同一个 marker 的 45 条轨迹明细，确认误报/漏报来自候选相位段、单条分割，还是最终投票。

5. 如果 blink marker 的 `fmcw_confirm_window_candidate_count=45` 且 pattern 稳定，而 large_motion 不稳定，再考虑把窗口 vote 变成 `confirm_require_vote` 的硬条件。

6. 如果 blink / large_motion 都稳定投成 blink，优先检查每条 trajectory 的 segmentation 中间产物，再考虑更细形态特征，而不是继续降低阈值。
