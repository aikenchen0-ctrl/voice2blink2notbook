# 迭代日志

日期：2026-06-13

## 项目目标

当前项目目标是用 MacBook 的扬声器和麦克风做主动声学眨眼检测。系统不依赖摄像头识别眨眼，摄像头只用于录像和人工标注验证。

整体路线从简单到复杂：

```text
单频 wave 大动作检测
-> 单频 blink 候选检测
-> FMCW 多轨可视化
-> FMCW 多轨候选轨迹、分割、投票
-> 连续实时眨眼检测
```

## 已完成内容

### 1. macOS 运行链路

项目已经可以在 macOS 上直接运行，主要命令：

```bash
.venv/bin/python scripts/run_hp_wave_detector.py --mode fmcw --session-root sessions --input-device 0 --output-device 1 --amplitude 0.02 --camera-width 640 --camera-height 480
```

当前本机设备：

```text
input : [0] MacBook Pro麦克风
output: [1] MacBook Pro扬声器
```

### 2. 旧版 wave/blink 检测

旧版逻辑大致是：

```text
音频特征 -> 单个 score -> rolling median/MAD 或类似阈值 -> 事件
```

其中：

- `wave` 更偏大动作检测；
- `blink` 是单频声波下的眨眼候选检测；
- 旧版页面有清晰的 score 曲线和 threshold 线；
- 从实测可视角度看，旧版目前比 FMCW 新版更稳定。

主要原因：

- 旧版是短窗口局部扰动检测；
- rolling median/MAD 能自动适应静止 baseline；
- 参数已经被本机实测数据修过；
- 输出只有一个 score，解释成本低。

### 3. FMCW 多轨可视化

新增 `--mode fmcw`。

默认 FMCW 参数：

```text
采样率：48000 Hz
扫频：18000 Hz -> 22000 Hz
period：512 samples
chirp：480 samples
guard：32 samples
decimation：16
```

页面底部实时显示 5 条 phase-difference track：

```text
14:15
14:16
14:17
14:18
14:19
```

这些轨迹用于观察眨眼和其他动作对不同相位对的影响。

### 4. FMCW 流式处理改进

已实现：

- 非整周期音频 chunk 缓冲；
- 每个 chirp 提取 phase points；
- rolling phase matrix；
- 默认保留 `180 x 30` 的相位矩阵，约 `1.92s`；
- 从 rolling phase matrix 中选 9 组 candidate bundle；
- 每组 5 条轨迹，总计 45 条 candidate trajectories；
- 轨迹去趋势、归一化、平滑；
- 单条轨迹 segmentation；
- hierarchical voting。

当前实时 FMCW 输出字段：

```text
fmcw_pattern
fmcw_vote_confidence
fmcw_vote_score
fmcw_group_winners
fmcw_candidate_count
```

### 5. CSV 和 marker 记录

`features.csv` 已记录：

```text
fmcw_track_0 ~ fmcw_track_4
fmcw_track_delta_rms
fmcw_phase_std
fmcw_pairs
fmcw_pattern
fmcw_vote_confidence
fmcw_vote_score
fmcw_group_winners
fmcw_candidate_count
```

`manual_markers.csv` 也会保存这些 FMCW 快照，便于对齐真实眨眼和干扰动作。

### 6. 测试状态

当前全量测试：

```text
29 passed
```

测试覆盖：

- chirp 生成；
- periodic signal wrapping；
- FMCW 分帧缓冲；
- rolling phase matrix；
- 45 条候选轨迹；
- 不稳定 phase point 规避；
- 轨迹预处理；
- segmentation；
- hierarchical voting；
- app 层 FMCW vote 更新；
- CLI 参数映射。

### 7. 实测数据

关键 session：

```text
sessions/hp_fmcw_20260613_170254
```

数据情况：

```text
时长：约 144.18s
features.csv：13518 行
manual_markers.csv：19 条标注
blink：13 条
large_motion：6 条
audio.wav：13MB
camera.mp4：94MB
```

初步观察：

- blink 标注附近的 `fmcw_track_delta_rms` 多数在 `0.08 ~ 0.12`；
- large_motion 有些也在类似范围；
- 最后两个大动作出现 `2.0+` 的 dRMS；
- 当前实时投票在 blink 附近触发不足；
- large_motion 末尾反而更容易输出 pattern，如 `11 / 13 / 3`。

## 当前判断

FMCW 方向是对的，但当前版本还不是可靠 blink detector。

旧版 `wave/blink` 当前可视效果更好，原因不是旧版理论上更强，而是它更像成熟的本地短窗口检测器：

```text
短窗口
单 score
自适应阈值
参数已经被实测修过
```

FMCW 当前更像刚搭好的多轨识别框架：

```text
信息更多
自由度更高
但还没被真实数据校准
```

下一步不应该直接用 `fmcw_pattern` 做事件，也不应该只调一个固定 `dRMS` 门槛。更合理的方向是：

```text
旧版短窗口检测负责候选触发
FMCW 多轨负责二次确认和抗干扰
```

## 主要问题

1. 当前 `fmcw_track_delta_rms` 固定门槛不够好。
2. 连续滑窗投票和论文的离线/命令式识别场景不同。
3. 1.92s 窗口可能混入头动、表情动作、按键延迟。
4. segmentation/voting 当前对大动作尾部也会产出 pattern。
5. 还没有真实 session 回放评估脚本。
6. 还没有基于标注数据的自动调参和回归测试。

## 2026-06-13 补充：真实回放分析脚本

新增：

```text
fmcw_session_analysis.py
scripts/analyze_fmcw_session.py
tests/test_fmcw_session_analysis.py
```

当前测试：

```text
31 passed
```

已对真实 session 生成报告：

```text
sessions/hp_fmcw_20260613_170254
docs/experiments/hp_fmcw_20260613_170254/
```

报告文件：

```text
marker_windows.csv
drms_threshold_sweep.csv
summary.json
```

关键结果：

```text
blink marker: 13
large_motion marker: 6
window: +-0.8s
```

`dRMS` 阈值 sweep 显示：

| threshold | blink recall | large motion trigger rate |
| ---: | ---: | ---: |
| 0.07 | 1.00 | 1.00 |
| 0.09 | 0.69 | 1.00 |
| 0.10 | 0.54 | 1.00 |
| 0.12 | 0.23 | 0.83 |
| 0.15 | 0.00 | 0.83 |
| 0.20 | 0.00 | 0.33 |

结论：

```text
单独 dRMS 阈值不能区分 blink 和 large_motion。
```

因此下一步应采用两阶段结构：

```text
候选触发器：召回优先
FMCW 二次确认：负责排除干扰
```

## 2026-06-13 补充：第二轮采集

新增 session：

```text
sessions/hp_fmcw_20260613_172442
docs/experiments/hp_fmcw_20260613_172442/
```

数据情况：

```text
features.csv: 5570 行
manual_markers.csv: 15 条标注
blink: 9
large_motion: 5
manual: 1
```

`dRMS` 阈值 sweep：

| threshold | blink recall | large motion trigger rate |
| ---: | ---: | ---: |
| 0.07 | 1.00 | 1.00 |
| 0.10 | 1.00 | 1.00 |
| 0.12 | 0.67 | 1.00 |
| 0.15 | 0.33 | 1.00 |
| 0.20 | 0.00 | 1.00 |
| 0.30 | 0.00 | 0.40 |

第二轮进一步确认：

```text
dRMS 可以作为候选触发的一部分，但不能区分眨眼和大动作。
```

与第一轮相比，第二轮 blink 的 dRMS 峰值更高，因此 `0.10` 可以召回全部 blink；但 large_motion 仍然全部触发。候选触发器应以召回为优先，后续必须引入二次确认特征。

## 2026-06-13 补充：FMCW candidate detector 壳

已把旧版 `wave/blink` 的核心实时检测思路接回 FMCW：

```text
fmcw_track_delta_rms
-> candidate_score
-> rolling median/MAD
-> candidate_threshold
-> fmcw_candidate event
```

新增配置：

```text
candidate_history_size
candidate_min_history
candidate_threshold_k
candidate_min_score
candidate_refractory_s
candidate_baseline_freeze_s
```

新增 CSV 字段：

```text
fmcw_candidate_score
fmcw_candidate_baseline
fmcw_candidate_mad
fmcw_candidate_threshold
fmcw_candidate_is_event
fmcw_candidate_event_id
```

UI 现在在 FMCW 多轨图上叠加：

```text
白线：candidate_score
橙线：candidate_threshold
```

这一步的意义是恢复旧版“短窗口 score + 自适应阈值”的可解释触发层。它仍然不是最终 blink 判定，因为两轮真实数据都显示：低阈值能召回 blink，但也会召回 large_motion。

当前测试：

```text
32 passed
```

## 2026-06-13 补充：candidate 回放和 confirm feature 第一版

已扩展回放分析：

```text
candidate_detector_summary.csv
candidate_detector_sweep.csv
confirm_features.csv
```

回放分析不依赖旧 session 已经有 `fmcw_candidate_*` 列，而是用 `fmcw_track_delta_rms` 重放同一套 rolling median/MAD detector。

默认 candidate detector 调整为召回优先：

```text
candidate_threshold_k = 3.0
candidate_min_score = 0.05
```

两轮真实 session 的 candidate 命中：

| session | blink hit | large motion hit |
| --- | ---: | ---: |
| hp_fmcw_20260613_170254 | 13/13 | 6/6 |
| hp_fmcw_20260613_172442 | 9/9 | 5/5 |

这说明 candidate 层已经能恢复旧版“灵敏触发”的优点，但也再次证明它不能直接作为 blink 判定。

第一版 `FMCWConfirmFeature` 字段：

```text
max_delta_rms
median_delta_rms
high_delta_duration_s
max_confidence
pattern_rows
dominant_pattern
pattern_stability
track_peak_width_s
track_sync_score
large_motion_suppressed
fmcw_confirmed
```

当前 confirm 初版结果：

| session | blink confirmed | large motion suppressed |
| --- | ---: | ---: |
| hp_fmcw_20260613_170254 | 13/13 中先命中 candidate；默认 confirm 只确认其中一部分，低幅 large_motion 仍会混入 |
| hp_fmcw_20260613_172442 | 7/9 confirmed | 5/5 suppressed |

批判：

```text
k=3 不是最终准确率提升，而是把候选召回做满。
第一轮低幅 large_motion 仍像 blink，需要继续找确认特征。
高幅/长持续 large_motion 已能用 max_delta_rms 和 high_delta_duration_s 压制。
```

## 2026-06-13 补充：实时最终 blink 判定层第一版

已把实时 FMCW 输出拆成三层：

```text
fmcw_candidate
fmcw_confirmed_blink
fmcw_suppressed_motion
```

含义：

```text
fmcw_candidate：疑似触发，只负责召回
fmcw_confirmed_blink：最终眨眼判定第一版
fmcw_suppressed_motion：被识别为干扰/大动作并压制
```

新增实时 confirm 字段：

```text
fmcw_confirm_state
fmcw_confirm_is_event
fmcw_confirm_event_id
fmcw_confirm_max_delta_rms
fmcw_confirm_high_delta_duration_s
fmcw_confirm_pattern
fmcw_confirm_confidence
```

UI 现在保留 blink 作为主目标，同时显示：

```text
candidate score / threshold
confirmed blink
suppressed motion
FMCW pattern / confidence
```

第一版判定策略：

```text
max_delta_rms >= 0.20 或 high_delta_duration_s >= 0.25
=> fmcw_suppressed_motion

0.05 <= max_delta_rms < 0.20 且 high_delta_duration_s < 0.25
=> fmcw_confirmed_blink
```

验证：

```text
35 passed
sessions_smoke/hp_fmcw_20260613_194930
```

smoke 中已写出：

```text
fmcw_candidate
fmcw_suppressed_motion
```

注意：

```text
这只是第一版最终判定层。
现在代码结构已经能区分 candidate 和 final blink，但阈值还需要继续用真实采集数据校准。
```

## 2026-06-14 补充：实时连续 vote-based confirm

根据 TwinkleTwinkle 原文，最终 pattern 不是单条阈值线，而是 45 条 candidate trajectories 分割后投票得到。因此实时 confirm 已推进为：

```text
fmcw_candidate
-> candidate window 内持续收集 FMCW vote summary
-> dominant pattern / confidence / stability
-> fmcw_confirmed_blink 或 rejected_vote / suppressed_motion
```

新增字段：

```text
fmcw_confirm_vote_score
fmcw_confirm_pattern_rows
fmcw_confirm_pattern_stability
```

新增 CLI：

```text
--fmcw-confirm-vote-min-confidence
--fmcw-confirm-vote-min-stability
```

当前确认条件：

```text
不是 large motion
max_delta_rms >= confirm_min_delta_rms
dominant pattern in ("1", "11", "12", "21")
max confidence >= confirm_vote_min_confidence
pattern stability >= confirm_vote_min_stability
```

验证：

```text
36 passed
sessions_smoke/hp_fmcw_20260614_133720
```

注意：

```text
这仍是实时连续近似，不是完整离线论文复刻。
下一轮实测重点看 rejected_vote 的原因：pattern_rows 是否不足、confidence 是否太低、stability 是否不稳定。
```

## 2026-06-15 补充：论文 4.3.3 异常方向 fallback 对齐

已把异常方向序列的 fallback 拆成论文对应的两步：

```text
segment samples
-> window=4 / step=2 variance profile
-> local high-low-high motif count
```

方向序列仍是主路径；只有 5 个方向、7 个方向等论文未列出的异常形态，才进入方差 fallback。论文没有给出 high/low 的绝对阈值，所以当前用 segment 内相对分位数和最小跨度约束，避免单侧高方差或平坦曲线被误认为眨眼。

验证：

```text
tests/test_fmcw_blink.py: 20 passed
```

## 2026-06-15 补充：论文第 5 节 criterion 分组投票对齐

第 5 节要求三个投票组分别来自 4.2 的三个 phase interval 选择准则：

```text
internal_similarity
slope_stability
same_point_temporal_consistency
```

已新增 `hierarchical_vote_by_groups`，`decode_candidate_bundles` 现在按 `bundle.criterion` 显式分组，而不是按 patterns 顺序三等分。这样即使后续调整 bundle 生成顺序，也不会破坏论文的三组投票语义。

验证：

```text
tests/test_fmcw_blink.py: 22 passed
```

## 2026-06-15 补充：论文 4.2 替代 reference 边界修正

论文 4.2 同时提到两点：

```text
index smaller than 15 可能受 low-pass filter 影响
重复 interval 时 reference 可从 t2-1 改成 t2-2 / t2-3
```

如果 target 刚好从有效区起点开始，直接用 `t2-2/t2-3` 会把 reference 拉回低通边界风险区。现在 `select_fmcw_candidate_bundles` 限制：

```text
reference_index >= valid_start - 1
```

默认 `valid_start=16` 时，reference 最早只到 15，target 仍从 16 开始。

验证：

```text
tests/test_fmcw.py: 32 passed
```

## 2026-06-15 补充：论文 4.2 同点跨 chirp 差值打分修正

论文 4.2 对第三类稳定性描述为：

```text
same index phase points on different chirps 的 differences
```

之前实现用相邻 chirp step 的方差，线性大漂移会因为步长一致而被误认为稳定。现在改为同一 phase index 在相邻 chirp 间的绝对差值总和：

```text
sum(abs(diff(phase[:, target_indices], axis=0)))
```

跨 `±pi` 的相位仍先 unwrap，所以不会把连续相位变化误当成大跳变。

验证：

```text
tests/test_fmcw.py: 33 passed
```

## 2026-06-15 补充：论文 4.2 重复 interval 退路记录

论文只说明：

```text
重复 interval 时 reference 从 t2-1 改成 t2-2 / t2-3
```

没有说明替代 reference 也不可用时怎么办。当前实现保持一个明确工程退路：

```text
如果替代 reference 越界或已经用过
-> 在同一个 criterion 的排序结果里继续找下一个 unused interval
-> 尽量保持每组 3 个 interval / 15 条 trajectory
```

这不是论文原文新增规则，而是为了连续实时系统和边界配置下的投票输入完整性。已补单测覆盖 `valid_start` 边界处不能回退 reference 时的 fallback 行为。

验证：

```text
tests/test_fmcw.py: 34 passed
```

## 2026-06-15 补充：论文 4.3.1 flat extrema 切边修正

论文 4.3.1 说 coarse segmentation 先按交替 local minima / maxima 切成 line segments。真实轨迹如果有平顶或平底 tip，直接用相邻差分符号变化会把极值放到平台左端，导致后续 edge length、representative diff 和 direction 边界偏移。

现在 `detect_extrema` 先把连续同值点压成 run，当前后趋势反转时取平台中点作为 extrema：

```text
[0, 0.4, 1, 1, 1, 0.2, -0.5, -0.5, 0.1]
-> extrema [0, 3, 6, 8]
```

验证：

```text
tests/test_fmcw_blink.py: 23 passed
```

## 2026-06-15 补充：论文 4.3.1 coarse edge 方向独立长度阈值

论文 4.3.1 对 coarse edge 的长度过滤是：

```text
less than 20% of the longest edge in each direction of each trial
```

也就是正向和负向分别找自己的 longest edge。当前 `filter_edges` 已经这样实现，本轮补了测试，防止未来误改成全局 longest edge。representative diff 阈值仍按论文使用全 trial 的最大 representative diff。

验证：

```text
tests/test_fmcw_blink.py: 24 passed
```

## 2026-06-15 补充：论文 4.3.1 representative diff top-3 均值测试

论文 4.3.1 定义 representative difference 为：

```text
average of the top 3 large difference values
```

当前 `edge_representative_difference` 已按 `abs(diff(edge_samples))` 排序后取 top 3 平均。本轮补单测锁住该语义，防止未来误改成单个最大值或全部 adjacent diff 的均值。

验证：

```text
tests/test_fmcw_blink.py: 25 passed
```

## 2026-06-15 补充：论文 4.3.1 trim 严格阈值

论文写的是：

```text
difference from the previous point is larger than 1/4 of the largest difference
```

因此 `trim_edge_starts` 的触发条件从 `>= threshold` 改为 `> threshold`。刚好等于阈值的差值不会触发 trim，下一处严格大于阈值的 sample 才会作为新起点。

验证：

```text
tests/test_fmcw_blink.py: 26 passed
```

## 2026-06-15 补充：论文 4.3.2 two-edge segment 规则测试

论文 4.3.2 中 `n_i == 2` 的 segment 保留条件是：

```text
两条边长度都 >= global max_length / 2
或任一 edge representative_diff >= global max_diff * 0.4
```

当前实现已经符合，本轮补测试覆盖“长度不够，但其中一条 edge diff 足够大，因此两条边都保留”的情况。

验证：

```text
tests/test_fmcw_blink.py: 27 passed
```

## 2026-06-15 补充：事件级准确率评估闭环

新增：

```text
event_evaluation.py
scripts/evaluate_events.py
tests/test_event_evaluation.py
```

评估逻辑：

```text
manual blink marker
-> 在 tolerance 窗口内一对一匹配最终 blink event
-> 统计 TP / FN / FP / recall / precision / F1
```

默认最终事件标签：

```text
FMCW session: fmcw_confirmed_blink
blink session: blink_candidate
```

注意：`fmcw_candidate` 只是召回优先的候选层，不计入最终眨眼准确率。

最近三组 FMCW session 的事件级总账：

```text
sessions/hp_fmcw_20260615_122844
sessions/hp_fmcw_20260615_121528
sessions/hp_fmcw_20260615_120719
```

结果：

| markers | events | TP | FN | FP | recall | precision | F1 | false discovery |
| ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 61 | 164 | 49 | 12 | 115 | 0.803 | 0.299 | 0.436 | 0.701 |

输出目录：

```text
docs/experiments/event_eval_baseline/
```

关键判断：

```text
当前确认层误报过高，不能宣称接近 95%。
下一步必须用 hybrid-split 先验证旧版单频 blink_score 是否恢复，再把 FMCW 约束为二次确认/大动作抑制。
```

当前全量测试：

```text
124 passed
```

## 2026-06-15 补充：hybrid-split 后 primary blink 波峰候选层

新采集 session：

```text
sessions/hp_fmcw_20260615_152200
```

标记：

```text
blink markers: 39
large_motion markers: 0
```

直接评估实时事件：

| event label | TP | FN | FP | recall | precision | F1 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| fmcw_confirmed_blink | 22 | 17 | 9 | 0.564 | 0.710 | 0.629 |
| blink_candidate | 8 | 31 | 7 | 0.205 | 0.533 | 0.296 |

人工观察“波峰和真实眨眼有相关性”是对的。离线窗口验证：

```text
39 个 blink marker 中，37 个在 +-0.8s 内有 blink_score 波峰超过阈值。
32 个超过 1.5 倍阈值。
```

定位到旧 primary detector 的问题：

```text
Twinkle peak gate 用 threshold * release_ratio 作为释放线。
hybrid-split 下 baseline 常在 0.04~0.06，release 线却约 0.02。
因此 active 很难释放，连续眨眼时后续峰值不再产生事件。
```

修复：

```text
release_level = baseline + (threshold - baseline) * release_ratio
```

新增可见波峰候选层：

```text
primary_blink_peak.py
primary_blink_evaluation.py
scripts/evaluate_primary_blink.py
tests/test_primary_blink_evaluation.py
```

该层直接使用页面可见的 `blink_score / blink_threshold`：

```text
blink_score local peak
score >= min_score
score / threshold >= min_ratio
refractory_s
-> blink_candidate
-> pending FMCW confirm
```

默认参数：

```text
primary_blink_peak_min_score = 0.06
primary_blink_peak_min_ratio = 1.0
primary_blink_peak_refractory_s = 1.05
```

在 `sessions/hp_fmcw_20260615_152200` 上：

| layer | events | TP | FN | FP | recall | precision | F1 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| primary_blink_peak | 49 | 37 | 2 | 12 | 0.949 | 0.755 | 0.841 |

这还没有达到最终目标，因为：

```text
0.949 只是候选层召回，且只在一轮 blink-only 数据上验证。
precision 仍只有 0.755，需要 FMCW/干扰标注继续压误报。
还没有 large_motion 标记，无法证明抗干扰。
```

当前全量测试：

```text
128 passed
```

## 2026-06-15 补充：波峰相关性和 confirm 压误报

用户实测观察到：

```text
页面上的 blink_score 波峰和真实眨眼已经有一定相关性。
```

离线评估确认这个观察成立。使用：

```bash
.venv/bin/python scripts/evaluate_primary_blink.py \
  sessions/hp_fmcw_20260615_152200 \
  --output-dir docs/experiments/hp_fmcw_20260615_152200_primary_blink_eval \
  --tolerance 0.8 \
  --ignore-startup 2.0 \
  --min-score 0.06 \
  --min-ratio 1.0 \
  --refractory 1.05
```

结果：

| layer | events | TP | FN | FP | recall | precision | F1 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| primary_blink_peak | 49 | 37 | 2 | 12 | 0.949 | 0.755 | 0.841 |
| primary_blink_peak + FMCW max_delta<=0.06 | 45 | 37 | 2 | 8 | 0.949 | 0.822 | 0.881 |

这说明当前最有效的实时链路不是让 FMCW vote 硬确认眨眼，而是：

```text
单频 blink_score 局部峰值
-> 作为主眨眼候选
-> FMCW 窗口只先压制明显异常/大动作
-> 输出 confirmed blink 或 suppressed motion
```

已做改动：

```text
scripts/evaluate_primary_blink.py 输出 best_confirm_sweep
primary_blink_evaluation.py 输出 primary_blink_windows.csv / primary_blink_confirm_sweep.csv
config.py 默认 confirm_large_motion_delta_rms 调为 0.06
cli.py 增加 confirm 阈值参数，便于下一轮不改代码 A/B
app.py 调整 confirm 顺序：明显大动作压制优先，vote 不再绕过大动作门
```

当前全量测试：

```text
129 passed
```

仍不能宣称达到 95%：

```text
0.949 是 blink-only session 上的候选/离线确认结果。
这轮没有 w 标注，不能证明对挥手、转头、张嘴、挑眉等干扰的最终准确率。
下一轮必须同时采 b 和 w，才能继续逼近 95% 目标。
```

## 2026-06-15 补充：第一轮 b/w 同采 session

新 session：

```text
sessions/hp_fmcw_20260615_171453
```

标注：

```text
blink: 45
large_motion: 4
```

离线评估：

```bash
.venv/bin/python scripts/evaluate_primary_blink.py sessions/hp_fmcw_20260615_171453 \
  --output-dir docs/experiments/hp_fmcw_20260615_171453_primary_blink_eval \
  --tolerance 0.8 --ignore-startup 2.0 --min-score 0.06 --min-ratio 1.0 --refractory 1.05

.venv/bin/python scripts/evaluate_events.py sessions/hp_fmcw_20260615_171453 \
  --output-dir docs/experiments/hp_fmcw_20260615_171453_event_eval \
  --tolerance 0.8 --ignore-startup 2.0

.venv/bin/python scripts/evaluate_negative_markers.py sessions/hp_fmcw_20260615_171453 \
  --output-dir docs/experiments/hp_fmcw_20260615_171453_negative_marker_eval \
  --tolerance 0.8 --ignore-startup 2.0
```

结果：

| layer | markers | events | TP | FN | FP | recall | precision | F1 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| primary_blink_peak | 45 | 94 | 38 | 7 | 56 | 0.844 | 0.404 | 0.547 |
| fmcw_confirmed_blink | 45 | 73 | 35 | 10 | 38 | 0.778 | 0.479 | 0.593 |

新增负样本评估：

```text
negative markers: 4
confirmed blink conflicts near w: 0
suppressed near w: 4
conflict_rate: 0.000
suppression_rate: 1.000
```

当前判断：

```text
w 大动作压制在这 4 个样本上有效。
本轮主要问题不是 w 附近误确认为眨眼，而是 b 召回下降和非 w 区间误报多。
primary 候选层从上一轮 0.949 recall 掉到 0.844，说明候选层还没有稳定达到 95%。
final 层 recall=0.778，不能作为最终 95% 方案。
```

新增文件：

```text
negative_marker_evaluation.py
scripts/evaluate_negative_markers.py
tests/test_negative_marker_evaluation.py
```

当前全量测试：

```text
130 passed
```

## 2026-06-15 补充：primary 强峰上限与事件诊断

本轮没有发现新的 session 目录；最新可用数据仍是：

```text
sessions/hp_fmcw_20260615_171453
```

新增离线诊断输出：

```text
primary_blink_event_diagnostics.csv
```

每个 primary 候选现在会记录：

```text
是否命中 blink
是否靠近 large_motion
前后候选间隔
是否 burst
blink_score / threshold / ratio
FMCW 窗口 max_delta / median_delta / vote 摘要
```

关键发现：

```text
高召回设置 min_ratio=0.85 时，candidate recall 可以到 0.956。
但 71 个 FP 中，靠近 w 的只有 5 个，主要问题是 66 个非 w 区间的孤立弱峰。
因此 w 大动作压制不是当前主瓶颈；主瓶颈是 primary blink_score 的普通误峰。
```

补回旧版思路中的 score 上限门控：

```text
primary_blink_peak_max_score = 0.25
```

实现细节：

```text
超过 max_score 的局部峰不报 blink。
同时它会消费 refractory，避免强扰动后的回落小峰立刻变成新的候选。
```

默认配置评估结果：

| session | setting | markers | events | TP | FN | FP | recall | precision | F1 |
| --- | --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| hp_fmcw_20260615_152200 | min_ratio=1.0 max_score=0.25 | 39 | 46 | 37 | 2 | 9 | 0.949 | 0.804 | 0.871 |
| hp_fmcw_20260615_171453 | min_ratio=1.0 max_score=0.25 | 45 | 77 | 38 | 7 | 39 | 0.844 | 0.494 | 0.623 |

高召回候选评估：

| session | setting | markers | events | TP | FN | FP | recall | precision | F1 |
| --- | --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| hp_fmcw_20260615_152200 | min_ratio=0.85 max_score=0.25 | 39 | 48 | 38 | 1 | 10 | 0.974 | 0.792 | 0.874 |
| hp_fmcw_20260615_171453 | min_ratio=0.85 max_score=0.25 | 45 | 97 | 43 | 2 | 54 | 0.956 | 0.443 | 0.606 |

高召回 + confirm sweep 最优可保持召回：

```text
hp_fmcw_20260615_171453:
max_delta=0.100
max_high_duration=0.000
max_score=0.250
recall=0.956
precision=0.462
F1=0.623
```

当前判断：

```text
score 上限能压掉强扰动误报，但远未达到最终目标。
若目标优先是 >=95% recall，应临时使用 min_ratio=0.85 的高召回候选。
但 precision 仍只有约 0.46，下一步必须专门处理非 w 区间的弱峰 FP。
不能宣称 95% 准确率已经完成。
```

## 2026-06-15 补充：绿色线峰形 sweep 与日志缺口

本轮新增：

```text
primary_blink_event_diagnostics.csv 增加绿色 blink_score 峰形字段：
- peak_prominence
- peak_prominence_ratio
- half_width_s
- rise_time_s / fall_time_s
- abs_baseline_slope
- pre_post_symmetry

新增离线输出：
- primary_blink_shape_sweep.csv
```

真实 session 结果：

```text
sessions/hp_fmcw_20260615_152200
高召回候选: TP=38 FN=1 FP=10 recall=0.974 precision=0.792
best_shape_sweep: recall=0.974 precision=0.809 F1=0.884

sessions/hp_fmcw_20260615_171453
高召回候选: TP=43 FN=2 FP=54 recall=0.956 precision=0.443
best_confirm_sweep: recall=0.956 precision=0.462 F1=0.623
best_shape_sweep: recall=0.956 precision=0.467 F1=0.628
```

结论：

```text
简单峰形特征只能把 b/w session precision 从 0.462 提到 0.467，提升很小。
TP 和非 w 弱峰 FP 在 score / prominence / half_width 上高度重叠。
因此“只靠绿色线局部形状”不是下一步主突破口。
```

同时发现一个日志缺口：

```text
FMCW features.csv 里有 twinkle_* 列，但过去 _fmcw_feature_row 没有写入 primary blink detector 的 metrics。
所以历史 FMCW session 无法复盘旧版 Twinkle gate 的 accepted/reject 原因。
```

已修复：

```text
app.py 现在保存 latest_fmcw_primary_blink_metrics。
FMCW feature row 会写入绿色线 detector 的 twinkle_* / blinklistener_* metrics。
下一轮采集后可以直接评估旧版 gate 条件是否比外层 PrimaryBlinkPeakGate 更好。
```

当前测试：

```text
tests/test_fmcw_app_integration.py tests/test_primary_blink_evaluation.py
31 passed
```

## 2026-06-15 补充：区分 FMCW 投票分数与相位差轨迹线

用户指出一个关键语义问题：

```text
FMCW 投票背后不是“一个分数”，而是一批 phase-difference trajectories。
投票输出 pattern/confidence；真正可观察的 FMCW 数据线应该是相位差轨迹。
```

已修正 UI/CSV 表达：

```text
青色线 fmcw_blink_vote_evidence:
  FMCW 投票置信度分数，只有当前 vote pattern 属于单次眨眼模式时才非零。

洋红线 fmcw_blink_trajectory_value:
  支持当前 blink vote pattern 的代表性归一化 phase-difference trajectory 最新点。
  这条才是“FMCW 中某个相位差随时间变化的数据线”，不是分数。
```

新增 CSV / marker 字段：

```text
fmcw_blink_trajectory_value
fmcw_blink_trajectory_pattern
fmcw_blink_trajectory_pair
fmcw_blink_trajectory_criterion
```

当前解释应更新为：

```text
绿色线 = 18.5 kHz 单频旧版 blink_score，仍是主候选触发线。
青色线 = FMCW 多轨投票出来的 blink 置信度分数。
洋红线 = 当前 blink vote 背后选中的 FMCW 相位差轨迹数据线。
白/紫线 = 泛运动/多轨整体运动强度，不能直接当眨眼线。
```

当前测试：

```text
.venv/bin/python -m pytest tests
139 passed
```

## 2026-06-15 补充：旧版 Twinkle gate 复盘工具与新 session 结论

补完 `logged_gate_evaluation.py`：

```text
原问题：
  直接把 twinkle_candidate_accepted > threshold 的每一行当事件，会把同一次触发连续 2 帧重复计数。

已修正：
  detect_logged_gate_events 默认按上升沿提取事件。
  新增 refractory_s 去重。
  新增 min_score / max_score，用于复盘旧版 gate + score ceiling。
```

新增测试：

```text
tests/test_logged_gate_evaluation.py
- 连续 active 行只算一个事件
- refractory 抑制近距离上升沿
- min_score / max_score 过滤
- session 级 marker 对齐评估
```

在唯一包含完整 `twinkle_*` metrics 的新 session 上复盘：

```text
sessions/hp_fmcw_20260615_180004
blink markers=51
large_motion markers=0
```

结果：

```text
PrimaryBlinkPeakGate 当前高召回配置:
  events=97 TP=47 FN=4 FP=50 recall=0.922 precision=0.485 F1=0.635

twinkle_candidate_accepted:
  events=73 TP=27 FN=24 FP=46 recall=0.529 precision=0.370 F1=0.435

twinkle_candidate_peak:
  events=144 TP=50 FN=1 FP=94 recall=0.980 precision=0.347 F1=0.513

twinkle_candidate_peak + blink_score <= 0.20:
  events=135 TP=50 FN=1 FP=85 recall=0.980 precision=0.370 F1=0.538
```

结论：

```text
旧版 accepted gate 过严，不能作为当前主候选源。
旧版 peak 层有足够召回，可作为高召回候选 fallback。
但 FP 非常多，单靠 blink_score 上限只能小幅改善。
下一步需要用 FMCW 洋红相位差轨迹 / phase pair 稳定性 / w 标注做二次确认，而不是再堆单分数阈值。
```

当前测试：

```text
.venv/bin/python -m pytest tests
143 passed
```

## 2026-06-15 补充：候选融合/补漏离线评估

新增：

```text
candidate_fusion_evaluation.py
scripts/evaluate_candidate_fusion.py
tests/test_candidate_fusion_evaluation.py
fused_candidate_diagnostics.csv
```

用途：

```text
保留 PrimaryBlinkPeakGate 事件；
twinkle_candidate_peak fallback 只在附近没有 primary 事件时补进；
同时输出 primary / fallback / fused 三套事件级指标；
统计 fallback 实际救回多少 blink marker、额外带来多少 FP。
并逐事件标注 source / matched / false_positive，方便下一轮叠加 FMCW 洋红相位差轨迹分析。
```

在 `sessions/hp_fmcw_20260615_180004` 上的结果：

```text
fallback_exclusion=0.8:
  primary: recall=0.922 precision=0.485 F1=0.635
  fallback: recall=0.980 precision=0.370 F1=0.538
  fused: recall=0.941 precision=0.397 F1=0.558
  fallback_added_events=24 rescued_markers=1 added_fp=23

fallback_exclusion=0.2:
  fused: recall=1.000 precision=0.242 F1=0.389
  fallback_added_events=114 rescued_markers=4 added_fp=94

fallback_exclusion=0.0:
  fused: recall=1.000 precision=0.220 F1=0.360
  fallback_added_events=135 rescued_markers=4 added_fp=111
```

结论：

```text
twinkle_candidate_peak fallback 确实能补 Primary 的漏检；
但按时间规则直接融合会显著增加误报。
因此 fallback 不能直接接入最终 blink 事件，只能进入“待 FMCW 二次确认”的候选池。
下一步必须用新加的 FMCW 洋红相位差轨迹 / 投票证据 / w 标注来判别 fallback TP vs fallback FP。
```

当前测试：

```text
.venv/bin/python -m pytest tests
145 passed
```

## 2026-06-20：视觉标注 session 的误报定位与参数扫描工具

最新采集：

```text
session: sessions/hp_fmcw_20260619_184345
manual_markers.csv: 33 blink, 0 large_motion/w
features.csv: 24145 rows, 0.159s-257.695s
visual_features.csv: 7006 rows
```

视觉门控事件评估：

```text
.venv/bin/python scripts/evaluate_events.py \
  sessions/hp_fmcw_20260619_184345 \
  --output-dir docs/experiments/hp_fmcw_20260619_184345_event_eval_visual_face \
  --require-visual-face

fmcw_confirmed_blink:
markers=33 events=161 TP=21 FN=12 FP=140
recall=0.636 precision=0.130 f1=0.216
```

候选融合评估：

```text
.venv/bin/python scripts/evaluate_candidate_fusion.py \
  sessions/hp_fmcw_20260619_184345 \
  --output-dir docs/experiments/hp_fmcw_20260619_184345_candidate_fusion_eval_visual_face \
  --require-visual-face

primary:  recall=0.788 precision=0.159 f1=0.264
fallback: recall=0.939 precision=0.140 f1=0.243
fused:    recall=0.879 precision=0.153 f1=0.261
fallback_added: events=25 rescued_markers=3 false_positives=22
negative_conflicts: markers=0 primary=0 fallback=0 fused=0
```

结论：

```text
这轮没有 w/large_motion 负样本，因此不能证明抗挥手/转头效果。
更大的问题是高召回下误报极多：33 个视觉眨眼对应 140+ FP。
```

新增离线扫描工具：

```text
candidate_fusion_sweep.py
scripts/sweep_candidate_fusion.py
tests/test_candidate_fusion_sweep.py
```

用途：

```text
一次读入 features/markers，缓存 primary/fallback 事件，批量扫描：
- primary_min_score
- primary_min_ratio
- primary_refractory
- fallback_min_score
- fallback_refractory
- fallback 是否启用

输出：
docs/experiments/<session>_candidate_fusion_parameter_sweep/candidate_fusion_parameter_sweep.csv
```

本轮扫描命令：

```text
.venv/bin/python scripts/sweep_candidate_fusion.py \
  sessions/hp_fmcw_20260619_184345 \
  --require-visual-face \
  --min-recall 0.85 \
  --output-dir docs/experiments/hp_fmcw_20260619_184345_candidate_fusion_parameter_sweep
```

扫描结论：

```text
rows=3800
recall >= 0.85 时，最佳 precision 仅约 0.172：
  recall=0.879 precision=0.172 f1=0.287 TP=29 FN=4 FP=140

recall >= 0.80 时，最佳 precision 约 0.194：
  TP=27 FN=6 FP=112

追求 precision 时最高约 0.556，但 recall 只有 0.303：
  TP=10 FN=23 FP=8
```

判断：

```text
当前不是简单调阈值能到 95% 的问题。
18.5kHz blink_score 在这次采集里可分性不足：
高召回参数会放出大量非眨眼峰；
高 precision 参数会漏掉大多数真实眨眼。
```

新增可观测字段：

```text
features.csv / manual_markers.csv 新增：
- blink_detector_event_id
- blink_detector_is_event
- blink_detector_method
- blink_peak_event_id
- blink_peak_is_event
- blink_peak_score
- blink_peak_threshold
- blink_peak_ratio
```

原因：

```text
实时混合链路里有两层：
1. 旧版 Twinkle detector 本体
2. 后加 PrimaryBlinkPeakGate local-peak 主候选

旧字段 blink_is_event 表示当前实际用于打开 confirm 的主候选，
但不足以区分误报来自 detector 本体还是后加 peak gate。
新字段用于下一次采集后做分层评估。
```

当前测试：

```text
.venv/bin/python -m pytest tests
188 passed
```

全仓 `pytest` 现状：

```text
.venv/bin/python -m pytest
被仓库内外部子目录打断：
- FaceAcousticSensing/median_dev_test.py 缺 torch
- voice2blink/generate_code/* 缺 yaml

这不是 hp_acoustic_wave/tests 当前改动失败。
```

## 2026-06-21：补齐 blink 分层诊断入口

新增：

```text
scripts/evaluate_blink_layers.py
```

目的：

```text
把同一个 session 的几条 blink 链路放在同一张表里比较：
- detector_is_event: 旧版 detector 本体事件
- peak_is_event: 后加 PrimaryBlinkPeakGate 事件
- current_blink_is_event: 当前实时主候选字段
- twinkle_accepted: 旧版 Twinkle gate 记录的 accepted
- offline_primary_peak: 离线 primary peak 复算
- candidate_fused: primary + fallback 融合
```

同时给 `evaluate_logged_gate.py` / `logged_gate_evaluation.py` 增加：

```text
--require-visual-face
```

这样分层指标能和 candidate fusion 的视觉门控指标使用同一套 visible-face 时间范围。

对旧 session `hp_fmcw_20260619_184345` 运行：

```text
.venv/bin/python scripts/evaluate_blink_layers.py \
  sessions/hp_fmcw_20260619_184345 \
  --require-visual-face \
  --output-dir docs/experiments/hp_fmcw_20260619_184345_blink_layer_eval
```

结果：

```text
detector_is_event       recall=0.000 precision=0.000 f1=0.000
peak_is_event           recall=0.000 precision=0.000 f1=0.000
current_blink_is_event  recall=0.000 precision=0.000 f1=0.000
twinkle_accepted        recall=0.758 precision=0.194 f1=0.309
offline_primary_peak    recall=0.788 precision=0.159 f1=0.264
candidate_fused         recall=0.879 precision=0.153 f1=0.261
```

解释：

```text
前三行是 0，不代表 detector 本体没有能力；
原因是这个旧 session 采集时还没有 blink_detector_* / blink_peak_* 新字段。

可用结论是：
旧版 twinkle_accepted 在这段数据上比 offline_primary_peak 更少误报：
  twinkle_accepted:     25 TP, 8 FN, 104 FP
  offline_primary_peak: 26 TP, 7 FN, 138 FP

下一轮采集后，新字段会直接告诉我们误报主要来自 detector 本体，还是后加 peak gate。
```

## 2026-06-21：新增采集后一键快评估

新增：

```text
blink_layer_evaluation.py
post_collection_evaluation.py
scripts/evaluate_post_collection.py
tests/test_post_collection_evaluation.py
```

`evaluate_blink_layers.py` 已改成复用 `blink_layer_evaluation.py`，避免脚本里复制评估逻辑。

用途：

```text
人类采集完成并说“好了”后，直接跑一条命令：

.venv/bin/python scripts/evaluate_post_collection.py <session_dir>

自动输出：
- visual_label_audit/
- blink_layers/
- candidate_fusion/
- candidate_fusion_parameter_sweep/
- post_collection_summary.csv
- summary.json
```

该快评估不跑重型 phase-pair ranking；它服务采集后的第一轮快速判断：

```text
1. 视觉标注是否可靠
2. 是否补够 w/large_motion 负样本
3. 误报主要来自哪一层 blink 链路
4. 当前 fused 是否接近 95% recall / precision 目标
5. 参数扫描在高召回下的 precision 上限
```

对 `hp_fmcw_20260619_184345` 运行：

```text
.venv/bin/python scripts/evaluate_post_collection.py \
  sessions/hp_fmcw_20260619_184345 \
  --output-dir docs/experiments/hp_fmcw_20260619_184345_post_collection_eval \
  --sweep-min-recall 0.85
```

输出摘要：

```text
markers blink=33 negative=0 needs_negative_labels=1
visual events=33 valid=33 valid_rate=1.000
layer_best twinkle_accepted recall=0.758 precision=0.194 f1=0.309
fused recall=0.879 precision=0.153 f1=0.261 fp=160 negative_conflicts=0
sweep_best min_recall=0.850 recall=0.879 precision=0.172 f1=0.287 fp=140
targets recall=0 precision=0
```

结论：

```text
这个 session 的视觉标注可靠，但没有 w 负样本；
高召回下 precision 仍明显不达标。
下一轮采集必须同时包含 blink 和 w，大动作时按终端 w。
```

## 2026-06-21：固定姿态采集结束后自动快评估

更新：

```text
scripts/run_fixed_posture_collection.py
```

采集入口现在在 detector 退出并拿到 session_dir 后，默认自动运行：

```text
evaluate_post_collection_session(session_dir)
```

默认输出：

```text
docs/experiments/<session>_post_collection_eval
```

终端会直接打印：

```text
Post-collection evaluation:
  output: ...
  markers: blink=<n> negative=<n> needs_negative_labels=<0/1>
  fused: recall=<r> precision=<p> fp=<n> negative_conflicts=<n>
  sweep_best: recall=<r> precision=<p> fp=<n>
```

新增参数：

```text
--no-post-eval
--post-eval-output-root <dir>
```

意义：

```text
以后“启动页面 -> 人类采集 -> 好了/按 q 结束”后，不需要再手动记多条评估命令。
采集脚本会自动给出当前 session 是否缺 w、precision/recall 是否达标、参数扫描是否有更好候选。
```

当前测试：

```text
.venv/bin/python -m pytest tests
191 passed
```

## 2026-06-15 补充：固定姿态采集进度显示

为了下一轮人工采集不再漏掉 `w` 标注数量，实时页面增加分项人工计数：

```text
manual_blink_marker_count
manual_large_motion_marker_count
collection_target_blinks
collection_target_negatives
```

固定姿态采集入口现在默认传：

```text
--collection-target-blinks 40
--collection-target-negatives 20
```

页面标题会显示类似：

```text
人工=17  b=12/40  w=5/20
```

metadata 也会记录：

```text
manual_blink_marker_count
manual_large_motion_marker_count
```

验证：

```text
.venv/bin/python -m pytest tests
165 passed
```

## 2026-06-15 补充：FMCW 物理线相关性 gate

新增 `fmcw_pair_ranking.py` 的 physical-line template-correlation 排名：

```text
phase pair 轨迹 -> blink 窗口模板 -> blink/background/w 相关性对比
```

它和旧 `shape/peak` 排名分开输出：

```text
旧口径 best_pair：看窗口 shape/peak 是否区分 blink 与背景
新口径 best_line_pair：看某条固定 phase-pair 物理轨迹是否稳定复现 blink 模板
```

新增输出：

```text
fmcw_pair_line_correlation_aggregate.csv
*_line_correlation.csv
summary.json:
  best_line_pair
  best_line_blink_hit_rate
  recommended_line_pair
  recommended_line_cli
```

在旧三段 session 上运行：

```bash
.venv/bin/python scripts/rank_fmcw_phase_pairs_many.py \
  sessions/hp_fmcw_20260615_171453 \
  sessions/hp_fmcw_20260615_180004 \
  sessions/hp_fmcw_20260615_185205 \
  --center-offset-sweep=-0.4:0.4:0.1 \
  --output-dir docs/experiments/fmcw_pair_line_correlation_20260615_171453_180004_185205
```

结果：

```text
shape/peak best_pair = 24:25, hit = 0.124, recommended = no
physical-line best_line_pair = 11:22, hit = 0.083, recommended_line_pair = null
```

固定姿态总评估已接入该 gate：

```text
docs/experiments/fixed_posture_study_line_gate_20260615_171453_180004_185205

markers blink=121 negative=4 phase_logs=0/3
physical_line recommended=None best=11:22 hit=0.083
fusion recall=0.942 precision=0.473 negative_conflicts=0
decision need_more_fixed_posture_labels
```

结论：

```text
当前旧数据仍不能证明存在可接入实时的固定 FMCW 眨眼物理线。
下一步必须采同一固定姿态下的 b/w 数据，并保留 fmcw_phase_points。
```

当前测试：

```text
.venv/bin/python -m pytest tests
163 passed
```

## 2026-06-15 补充：固定姿态采集入口

新增：

```text
scripts/run_fixed_posture_collection.py
```

用途：

```text
封装下一轮固定姿态 b/w 采集的默认启动参数：
  --mode fmcw
  --fmcw-band-preset hybrid-split
  --no-video-record
  --profile
  保留 fmcw_phase_points

脚本会拒绝 --fmcw-no-phase-point-log，避免采集后无法直接做 phase-pair ranking。
```

启动命令：

```bash
.venv/bin/python scripts/run_fixed_posture_collection.py \
  --input-device 0 \
  --output-device 1 \
  --amplitude 0.02
```

采集目标：

```text
b: 至少 40 次 blink
w: 至少 20 次 large_motion / interference
固定头部、电脑、扬声器/麦克风位置直到结束
必须等人类说“好了”后再停止和评估
```

当前测试：

```text
.venv/bin/python -m pytest tests
161 passed
```

## 2026-06-15 补充：固定姿态总评估入口

新增：

```text
fixed_posture_study.py
scripts/evaluate_fixed_posture_study.py
```

这个脚本把下一轮固定姿态采集后的验收整合成一个入口：

```text
1. 检查每个 session 的 blink / w 标注数量。
2. 检查是否保留 fmcw_phase_points。
3. 跑 phase-pair shape ranking + center_offset sweep。
4. 输出全局模板和 per-session 模板 summary。
5. 跑 candidate fusion 聚合评估。
6. 生成 fixed_posture_study_summary.json，给出状态和下一步建议。
```

对现有三段旧数据跑基线：

```bash
.venv/bin/python scripts/evaluate_fixed_posture_study.py \
  sessions/hp_fmcw_20260615_171453 \
  sessions/hp_fmcw_20260615_180004 \
  sessions/hp_fmcw_20260615_185205 \
  --output-dir docs/experiments/fixed_posture_study_baseline_20260615_171453_180004_185205
```

结果：

```text
blink markers = 121
negative/w markers = 4
phase_point_log_sessions = 0/3
phase_pair recommended_pair = null
phase_pair best_pair = 24:25
phase_pair blink_hit_rate = 0.124
candidate_fusion fused recall = 0.942
candidate_fusion fused precision = 0.473
candidate_fusion negative_conflicts = 0
decision = need_more_fixed_posture_labels
```

解释：

```text
这组旧数据不能证明 95% 目标：
  w 标注远少于 20；
  没有直接保留 fmcw_phase_points；
  FMCW 没有 recommended_pair；
  fused recall 低于 0.95。

下一轮固定姿态采集完成后，直接运行这个总评估脚本作为验收入口。
```

当前测试：

```text
.venv/bin/python -m pytest tests
159 passed
```

## 2026-06-15 补充：模板诊断 summary

新增：

```text
fmcw_template_diagnostics.py
scripts/analyze_fmcw_pair_templates.py
```

用途：

```text
读取 fmcw_pair_templates.csv，输出 fmcw_pair_template_summary.csv：
  peak_relative_time
  peak_value
  span
  endpoint_delta
  sign
  rms
  blink_distance
```

对本轮 offset sweep 的最佳 pair 模板运行：

```bash
.venv/bin/python scripts/analyze_fmcw_pair_templates.py \
  docs/experiments/fmcw_pair_rank_shape_offset_20260615_171453_180004_185205
```

结果：

```text
background: span=0.1564 peak_t=-0.82 endpoint=-0.0165 sign=positive blink_dist=0.0555
blink:      span=0.0990 peak_t=-0.78 endpoint=-0.0187 sign=positive blink_dist=0.0000
negative:   span=0.4192 peak_t=+0.93 endpoint= 0.0185 sign=positive blink_dist=0.0845
```

解释：

```text
当前 best pair 的 blink 模板并不比 background 更强；
negative 模板 span 反而最大。

这进一步说明：
  现在的 FMCW 物理轨迹还没有形成稳定 blink 特征；
  不能把当前 best pair 接入实时页面当最终眨眼线；
  下一步应看 per-session 模板，而不是继续在聚合平均上调阈值。
```

当前测试：

```text
.venv/bin/python -m pytest tests
156 passed
```

## 2026-06-15 补充：per-session 模板诊断

继续排查：

```text
聚合模板弱，是因为不同 session 相互抵消，还是每个 session 本身就弱？
```

新增输出：

```text
rank_fmcw_phase_pairs_many.py 现在额外输出：
  fmcw_pair_template_summary_by_session.csv
```

对三段数据的 per-session 最优 pair：

```text
hp_fmcw_20260615_171453:
  best_pair = 24:25
  separation = 0.059
  blink_hit_rate = 0.000
  blink span = 0.0179
  blink sign = negative
  negative span = 0.4192

hp_fmcw_20260615_180004:
  best_pair = 14:16
  separation = 0.252
  blink_hit_rate = 0.059
  blink span = 0.0470
  blink sign = positive

hp_fmcw_20260615_185205:
  best_pair = 16:29
  separation = 1.076
  blink_hit_rate = 0.280
  blink span = 0.0433
  blink sign = negative
```

结论：

```text
1. per-session 最优 pair 不一致。
2. blink 模板方向也不一致：171453/185205 为 negative，180004 为 positive。
3. 171453 是唯一含 w 的新近大样本之一，但 blink 模板极弱，negative 模板极强。
4. 185205 是 blink-only session，单 session 表现最好，但仍只有 hit=0.28，且无法证明抗干扰。

所以当前不是“聚合平均把好轨迹抵消了”这么简单；
更像是声路/姿态/同步/phase point 选择没有稳定锁定眼部微动。
```

当前测试：

```text
.venv/bin/python -m pytest tests
157 passed
```

## 2026-06-15 补充：center_offset sweep 与轨迹模板

继续排查：

```text
shape ranking 不达标，是否因为人工按 b 的时间与真实眨眼峰值有固定偏移？
```

新增能力：

```text
collect_phase_pair_peaks 支持 center_offset_s。
rank_fmcw_phase_pairs.py 支持 --center-offset。
rank_fmcw_phase_pairs_many.py 支持：
  --center-offset-sweep=-0.4:0.4:0.1
  --template-points 81

新增输出：
  fmcw_pair_rank_offset_sweep.csv
  fmcw_pair_templates.csv
```

运行：

```bash
.venv/bin/python scripts/rank_fmcw_phase_pairs_many.py \
  sessions/hp_fmcw_20260615_171453 \
  sessions/hp_fmcw_20260615_180004 \
  sessions/hp_fmcw_20260615_185205 \
  --metric shape \
  --center-offset-sweep=-0.4:0.4:0.1 \
  --output-dir docs/experiments/fmcw_pair_rank_shape_offset_20260615_171453_180004_185205 \
  --top 15
```

结果：

```text
selected_center_offset_s = +0.300
best_pair = 24:25
separation = 0.193
blink_hit_rate = 0.124
background_trigger_rate = 0.050
negative_trigger_rate = 0.000
recommended_pair = null
```

解释：

```text
人工标记时间偏移确实影响结果；
从 center_offset=0 的 separation=0.102 提升到 +0.3s 的 0.193。

但 blink_hit_rate 仍只有 12.4%，离 95% 目标相差很远。
所以问题不是单纯“按 b 慢/快了”，更可能还包括：
  - 当前 phase-pair 物理轨迹本身与眨眼耦合不稳定；
  - shape score 还没捕捉到真正稳定的眨眼形态；
  - 多 session 之间最优 pair 不一致，说明声路/姿态/同步仍在漂。
```

当前测试：

```text
.venv/bin/python -m pytest tests
155 passed
```

## 2026-06-15 补充：phase-pair ranking 增加窗口形态评分

继续推进“FMCW 中是否存在眨眼物理轨迹线”的问题。

新增：

```text
fmcw_pair_ranking.py 支持 metric：
  delta_peak：旧指标，统计相邻 chirp 差分峰值。
  shape：新指标，统计窗口内轨迹形态。

shape 评分包含：
  - trajectory span
  - 端点回落程度
  - 导数方向反转次数
  - 极值是否落在窗口内部，而不是贴边漂移
```

单 session 对比：

```text
sessions/hp_fmcw_20260615_185205

delta_peak:
  best pair = 23:29
  separation = 0.19
  blink_hit_rate = 0.00

shape:
  best pair = 24:29
  separation = 0.51
  blink_hit_rate = 0.20
```

三段聚合：

```bash
.venv/bin/python scripts/rank_fmcw_phase_pairs_many.py \
  sessions/hp_fmcw_20260615_171453 \
  sessions/hp_fmcw_20260615_180004 \
  sessions/hp_fmcw_20260615_185205 \
  --metric shape \
  --output-dir docs/experiments/fmcw_pair_rank_shape_20260615_171453_180004_185205 \
  --top 15
```

结果：

```text
best_pair = 28:29
separation = 0.102
blink_hit_rate = 0.116
background_trigger_rate = 0.050
negative_trigger_rate = 0.000
recommended_pair = null
```

结论：

```text
shape ranking 比 delta_peak 更接近“眨眼是一段开闭轨迹”的物理含义，也提高了分离度；
但现有数据上仍远低于可接入实时的推荐门槛。
下一步应继续看“窗口内对齐/归一化/marker 偏移”的问题，而不是直接把 28:29 写成默认线。
```

当前测试：

```text
.venv/bin/python -m pytest tests
153 passed
```

## 2026-06-15 补充：FMCW 最相关相位轨迹 ranking

本轮目标是回答：

```text
FMCW 内部是否已经存在一条与眨眼高度相关、可直接接到实时页面的物理轨迹线。
```

完成的改动：

```text
新增 fmcw_pair_ranking.py：
  - 从 phase matrix 重建 phase-pair trajectory
  - target 限定在 valid_start..valid_stop，有效区间对齐实时/论文链路
  - 统计 blink 窗口峰值、background P95、negative/w 窗口 P95
  - 输出 separation、blink_hit_rate、background_trigger_rate、negative_trigger_rate

新增 scripts/rank_fmcw_phase_pairs_many.py：
  - 支持多个 session 聚合排名
  - 输出每个 session 的 pair rank
  - 输出 fmcw_pair_rank_aggregate.csv 和 summary.json
  - 只有达到最低 hit-rate / separation 才给 recommended_pair

修正 scripts/rank_fmcw_phase_pairs.py：
  - 旧脚本不再把 target < valid_start 的 09:10 等无效 pair 排进 top。

实时页面：
  - 新增 --fmcw-fixed-trajectory-pair，例如 23:29
  - 洋红固定 mm 轨迹优先使用该 pair
  - 未配置时仍沿用第一条 realtime track，兼容旧行为
```

对现有三段数据运行：

```bash
.venv/bin/python scripts/rank_fmcw_phase_pairs_many.py \
  sessions/hp_fmcw_20260615_171453 \
  sessions/hp_fmcw_20260615_180004 \
  sessions/hp_fmcw_20260615_185205 \
  --output-dir docs/experiments/fmcw_pair_rank_20260615_171453_180004_185205 \
  --top 8
```

结果：

```text
best_pair = 10:27
separation = 0.035
blink_hit_rate = 0.00
background_trigger_rate = 0.01
negative_trigger_rate = 0.25
recommended_pair = null
```

解释：

```text
当前数据里没有任何 FMCW phase-pair 通过“可推荐眨眼线”的最低标准。
也就是说，现阶段不能说 FMCW 已经找到了一条稳定眨眼物理轨迹；
页面上的青色投票/蓝色代表/洋红固定线都还不能替代旧版绿色 blink_score。
```

当前测试：

```text
.venv/bin/python -m pytest tests
152 passed
```

## 2026-06-15 补充：跨 session 聚合评估与候选默认参数更新

新增：

```text
scripts/evaluate_candidate_fusion_many.py
```

用途：

```text
一次输入多个 session；
每个 session 输出一套 candidate fusion 明细；
同时输出跨 session 总表：
- candidate_fusion_aggregate_strategy_metrics.csv
- candidate_fusion_aggregate_fmcw_sweep.csv
- candidate_fusion_aggregate_summary.json
```

聚合指标同时包含：

```text
blink recall / precision / FP
large_motion/w negative_conflict_total
```

在两个当前最有参考价值的历史 session 上：

```text
sessions/hp_fmcw_20260615_171453
sessions/hp_fmcw_20260615_180004
```

旧默认候选参数：

```text
primary_min_score=0.06
primary_min_ratio=0.85
primary_refractory=1.05
```

聚合结果：

```text
fused recall=0.948
precision=0.417
FP=127
negative_conflicts=0
```

小范围 sweep 后选定新默认：

```text
primary_min_score=0.04
primary_min_ratio=0.80
primary_refractory=1.20
primary_max_score=0.25
```

新默认聚合结果：

```text
primary: recall=0.938 precision=0.486 FP=95
fused:   recall=0.958 precision=0.442 FP=116
negative markers=4
negative conflicts=0
```

这说明：

```text
当前默认已经在历史聚合数据上超过 95% blink recall；
但 precision 仍低，FP 仍多；
且 large_motion/w 标注只有 4 个，不能证明最终抗干扰达标。
```

因此状态不是完成，而是：

```text
召回基线过 95%；
下一步必须采带 fmcw_blink_trajectory_value 的 b/w 新 session，
用同一套 aggregate 表验证 FP 和 negative_conflicts。
```

当前测试：

```text
.venv/bin/python -m pytest tests
146 passed
```

## 2026-06-15 补充：新增固定 FMCW phase-pair 轨迹诊断线

基于论文原文重新确认：

```text
论文没有定义“投票后选一条代表轨迹并画成连续线”；
论文主链路是 45 条 phase-pair trajectories 各自分割解码，再做三组层级投票。
```

因此当前 UI 里原有的洋红线需要谨慎解释：

```text
fmcw_blink_trajectory_value
= 当前滑窗投票赢家为 blink 时，从支持赢家的轨迹中选 span 最大者，并取最后一个值。
```

它不是固定物理轨迹，可能随窗口切换 pair / criterion。

新增诊断字段：

```text
fmcw_fixed_trajectory_value
fmcw_fixed_trajectory_phase_rad
fmcw_fixed_trajectory_distance_mm
fmcw_fixed_trajectory_pair
```

实现含义：

```text
固定使用实时 track 的第一组 phase pair；
从 rolling phase matrix 重算论文式 phase_difference_trajectory；
流程是 unwrap -> phase subtraction -> smoothing；
不 normalize，不使用投票结果；
按论文 Eq. 8 把相位差换算成相对等效距离 mm；
不参与事件触发，只用于可视化和离线诊断。
```

UI 更新：

```text
洋红：固定 phase-pair 的相对等效距离轨迹，单位 mm
蓝色：投票代表轨迹末端值，仅作辅助观察
```

下一轮标注时要比较：

```text
如果洋红固定 mm 轨迹对眨眼有峰，但蓝色/青色不稳定：
  问题更可能在 45 轨迹选择 / segmentation / vote / representative selection。

如果洋红固定 mm 轨迹和所有 track 都没有峰：
  问题更可能在 FMCW phase extraction、同步、频段、音频链路或手机/电脑几何位置。
```

当前测试：

```text
.venv/bin/python -m pytest tests
148 passed
```

## 2026-06-15 补充：洋红线改为固定物理量轨迹

修正前：

```text
洋红线 = fmcw_blink_trajectory_value
= 投票赢家为 blink 时，从支持赢家的轨迹里选 span 最大者，并取最后一个点。
```

问题：

```text
它会随滑窗切换 phase pair / criterion；
只取末端值，不是完整连续轨迹；
经过 normalize，不能解释为物理量。
```

修正后：

```text
洋红线 = fmcw_fixed_trajectory_distance_mm
= 固定 phase-pair 的相对等效距离轨迹，单位 mm。
```

计算方式：

```text
rolling phase matrix
-> 固定 reference:target pair
-> chirp 内 unwrap
-> phase subtraction
-> 时间方向 unwrap
-> smoothing
-> 不 normalize
-> 减 rolling window 中位数作为相对零点
-> 按论文 Eq. 8 换算成 distance_mm
```

新增字段：

```text
fmcw_fixed_trajectory_phase_rad
fmcw_fixed_trajectory_distance_mm
```

保留：

```text
fmcw_blink_trajectory_value
```

但它现在只作为“投票代表轨迹末端值”的辅助诊断，不再作为主可视化物理线。

UI：

```text
洋红：固定 mm 物理轨迹
蓝色：投票代表末端值
青色：投票分数
```

`candidate_fusion_evaluation.py` 的诊断表也新增：

```text
max_abs_fmcw_fixed_trajectory_distance_mm
nearest_fmcw_fixed_trajectory_distance_mm
max_abs_fmcw_fixed_trajectory_phase_rad
nearest_fmcw_fixed_trajectory_phase_rad
```

当前测试：

```text
.venv/bin/python -m pytest tests
149 passed
```

## 2026-06-15 补充：候选融合评估加入负样本冲突

`candidate_fusion_evaluation.py` 已扩展，不再只看 blink recall / FP，也统计候选事件是否落在 `large_motion/w` 标注附近。

新增 CSV 字段：

```text
candidate_fusion_summary.csv:
- negative_marker_total
- primary_negative_conflict_total
- fallback_negative_conflict_total
- fused_negative_conflict_total

candidate_fusion_fmcw_sweep.csv:
- negative_marker_total
- negative_conflict_total
- negative_conflict_rate
```

CLI 输出也新增：

```text
negative_conflicts markers=<n> primary=<n> fallback=<n> fused=<n>
best_fmcw_sweep ... neg_conflicts=<n>
```

在历史 session `hp_fmcw_20260615_171453` 上，当前基线为：

```text
primary/fused: recall=0.956, precision=0.443, FP=54
negative markers: 4
negative conflicts: 0
```

解释：

```text
这段数据已经达到 95% 以上 blink recall；
但 FP 仍然多，且只有 4 个 w 标注，不能证明总体抗干扰达标。
下一轮必须采带 fmcw_blink_trajectory_value 的 b/w session，用同一张 sweep 表同时看 recall、FP 和 negative_conflicts。
```

当前测试：

```text
.venv/bin/python -m pytest tests
145 passed
```

## 2026-06-15 补充：采集等待规则与当前数据状态

人工标注 session 的协作规则已固定：

```text
启动页面
-> 人类按 b/w 标注
-> 必须等人类明确说“好了”
-> 才能停止、评估、调参
```

当前检查结果：

```text
采集进程：未发现正在运行的 run_hp_wave_detector.py
最新 session：sessions/hp_fmcw_20260615_185205
manual_markers.csv：25 条标注
label 分布：25 blink，0 large_motion/w
features.csv：包含 fmcw_blink_vote_evidence 和 fmcw_blink_trajectory_value
```

由于这一轮没有收到“好了”，暂不把 `hp_fmcw_20260615_185205` 纳入正式离线评估。

现有可用于 b/w 干扰验证的 session：

```text
hp_fmcw_20260613_170254: 13 blink, 6 large_motion
hp_fmcw_20260613_172442: 9 blink, 5 large_motion
hp_fmcw_20260613_193543: 7 blink, 4 large_motion
hp_fmcw_20260615_171453: 45 blink, 4 large_motion
```

限制：

```text
较早 b/w session 多数没有 fmcw_blink_trajectory_value；
它们能验证旧候选/门控思路，但不能完整验证“投票代表轨迹”对误报的压制效果。
```

当前测试：

```text
.venv/bin/python -m pytest tests
145 passed
```

## 2026-06-15 补充：fallback 诊断接入 FMCW 证据 sweep

继续扩展 `candidate_fusion_evaluation.py`：

```text
fused_candidate_diagnostics.csv 现在每个事件额外输出：
- max_fmcw_blink_vote_evidence
- nearest_fmcw_blink_vote_evidence
- max_abs_fmcw_blink_trajectory_value
- nearest_fmcw_blink_trajectory_value
- dominant_fmcw_blink_trajectory_pair
- fmcw_blink_trajectory_pair_stability
- dominant_fmcw_blink_trajectory_pattern
- dominant_fmcw_blink_trajectory_criterion
- max_fmcw_track_delta_rms
- max_fmcw_confirm_window_confidence
```

新增输出：

```text
candidate_fusion_fmcw_sweep.csv
```

该 sweep 逻辑：

```text
primary 事件全部保留；
fallback 事件只有同时满足：
  max_fmcw_blink_vote_evidence >= min_vote_evidence
  max_abs_fmcw_blink_trajectory_value >= min_abs_trajectory_value
  fmcw_blink_trajectory_pair_stability >= min_pair_stability
才进入 fused 评估。
```

在旧 session `hp_fmcw_20260615_180004` 上重新运行：

```text
老 session 没有 fmcw_blink_trajectory_* 字段；
因此 best_fmcw_sweep 无法达到 recall >= 0.95。
这不是算法结论，只说明必须重新采集带洋红线字段的新 b/w session。
```

当前测试：

```text
.venv/bin/python -m pytest tests
145 passed
```
